# 视频推荐与热度算法讲解

> 对应代码：
> - 推荐主流程：`MediaServer-live/MediaServer/src/clogic.cpp` 的 `DownloadRq` → `GetFileList`（四级填坑）+ 辅助函数 `FetchVideos`
> - 热度计算入口（唯一）：`clogic.cpp` 的 `PlayReportRq`（播放上报实时重算）——**自 v6/v7/v8 起，曝光和点击不再改动 hotdegree，hotdegree 仅由播放上报单源驱动**
> - "看过"记录入口：`clogic.cpp` 的 `VideoClickRq`（v9 起：点击播放时写 `t_UserRecv` + `recvTime`，先删后插实现 LRU）
> - `MediaServer-live/MediaServer/tools/recompute_hotdegree.sql`：离线维护工具，非必需，用于一次性批量重刷全站热度（注意要同时重算 playCount，见 3.5）
>
> 触发时机：推荐流程——客户端登录成功后自动请求一次；用户点"刷新"按钮时再请求一次。热度更新——**只在播放上报（切视频/放完/关窗口）时重算**，由 `t_VideoPlayLog` 的近30天播放明细单条 SQL 同时派生 `playCount` 与 `hotdegree`；曝光与点击不再影响热度（见下方变更记录）。

> **变更记录（按时间顺序，面试被问"热度怎么设计的"可以整段讲）**：
> 1. 项目里一度加过"点赞"功能（`t_VideoLike` 表、`likeCount` 字段），后来因为功能跑不通已经整体回退，热度不吃点赞信号。
> 2. 热度最早是"曝光即 `+1`"，后来发现这样会有马太效应（越热越容易被推、越推越热），改成基于播放时长/完播率的批处理（`recompute_hotdegree.sql` + crontab）。
> 3. crontab 部署链路太长排查不动，改成播放上报到达时**同步实时重算**（代码内嵌进 `PlayReportRq`）。
> 4. 实时重算代码逻辑没问题，但联调时播放上报一直"发了等于没发"——`playCount`/`hotdegree` 长期是0，一度怀疑过登录状态、触发时机、编译部署，都不是。于是先把"曝光+1"重新加回来做保底，保证至少有信号在动。
> 5. **后来定位到真正的根因**：`STRU_PLAY_REPORT_RQ` 的构造函数只是把整个结构体 `memset` 清零，并没有把 `m_nType` 设成 `_DEF_PACK_PLAY_REPORT_RQ`；而客户端 `playerdialog.cpp` 发送这个包之前，从来没有手动补上这一行。服务端 `TCPKernel.cpp::DealData` 的分发逻辑是 `if (type >= _DEF_PACK_BASE && type < _DEF_PACK_BASE + _DEF_PACK_COUNT)` 才会往下派发，包类型是 0（远小于 `_DEF_PACK_BASE`=10000）直接被这行校验挡在外面、静默丢弃，连日志都不会打——这就是为什么无论怎么调整触发流程、检查登录状态、重新编译部署，服务端永远看不到 `[PlayReportRq]` 这行日志。修好之后（补上 `rq.m_nType = _DEF_PACK_PLAY_REPORT_RQ;`），这条链路才第一次真正跑通。
> 6. 同时新增了一个更简单可靠的信号——**点击播放上报**（`VideoClickRq`，双击卡片瞬间发送，不依赖播放时长/是否播完，触发条件比播放上报少得多），当时设计是热度 `+5`。
> 7. **v6（去掉曝光注水）**：联调发现"播一个视频、其他被推荐过的视频热度也跟着涨"——根因是 `GetFileList` 在 `nCount==0`（刷到底）时 `DELETE t_UserRecv` 全清后重新推送，导致一大批视频被重新曝光、各自 `+1`。改为仅在"全部推完"时重置，且**移除 `GetFileList` 里的 `hotdegree+1` 曝光注水**（曝光只做去重入表，不再加分）。
> 8. **v7（去掉点击注水）**：又发现"没播放热度也涨"——根因是 `VideoClickRq` 双击卡片就 `hotdegree+5`。改为 `VideoClickRq` 只记录点击、不再改动 hotdegree。至此**曝光与点击彻底退出热度计算**。
> 9. **v8（消除 playCount 双计数器漂移 + 中枢惩罚式公式）**：原来 `PlayReportRq` 里既有独立 `playCount = playCount + 1` 累加，又有覆盖式重算 hotdegree，两套计数器会漂移（如播放上报丢失时 playCount 漏加但 hotdegree 照算）。改为**单条 SQL 从 `t_VideoPlayLog` 同时派生 `playCount`（全量计数）和 `hotdegree`**，且 hotdegree 改用中枢惩罚式：`GREATEST(0, ROUND(playCount30d * 25 * (avgCompletion30d - 0.3)))`——完播率 >0.3 才加分（权重25/次），<=0.3（看一眼就关）整体算负被钳到 0，实现"不感兴趣则热度不涨/清零"，且热度恒非负。
> 10. **v9（当前版本：`t_UserRecv` 语义从"推过"改为"看过" + 四级填坑 + 换一批）**：产品语义调整——"推给你但你没点开"的视频刷新后应该还能出现，只有**真点开播放过**的才排除。`t_UserRecv` 改由 `VideoClickRq` 写入（点击播放瞬间，先删后插刷新 `recvTime`，LRU 语义），`GetFileList` 推送不再写表。同时废弃 v6 的"兜底模式"，改为**四级填坑保证每次刷新凑满 10 个坑位**：①没看过+不在上一批（7标签匹配+3纯热度）→②没看过但上一批推过→③看过的按 `recvTime` 最早优先补位→④全部看完则清空 `t_UserRecv` 重开循环。"换一批"靠内存中 `m_mapLastPushed`（带 `m_recMutex` 锁）记住上一批 10 条、下次优先避开。两段重复的解析循环抽成了 `FetchVideos()` 辅助函数（含批内去重 + 缩略图非空校验 + `picName/picPath <> ''` 脏数据过滤）。
>
> 现在是**播放上报单源驱动** hotdegree：点击写"看过"表（驱动推荐去重），播放上报驱动热度；两者数据链路彻底分离。

---

## 一、这个推荐策略到底是什么

一句话总结：**"标签匹配度 + 热度"的混合排序，外加"看过才排除"的去重表、"换一批"轮转、四级填坑凑满坑位、一个反信息茧房的兜底坑位**。不涉及机器学习、不涉及协同过滤，是纯规则（rule-based）的推荐逻辑，用 SQL 直接算分排序。

划分成四块能力：

1. **基于内容的推荐（Content-based Filtering）**：拿用户的 8 个兴趣标签（`food/funny/ennegy/dance/music/video/outside/edu`，注册时勾选）去匹配视频自带的同名 8 个标签，匹配上的越多分越高——这是标准的"用户画像 ↔ 物品画像"匹配，属于基于内容推荐的最简单形式。
2. **热度排序（Popularity-based）**：`t_VideoInfo.hotdegree` 字段记录热度，匹配度打平后用热度做二级排序，热度也单独撑起"3 个纯热度坑位"的排序依据。hotdegree 现在**只由播放上报（PlayReportRq）单源驱动**，按"近30天完播率中枢惩罚式"计算，见第三章。
3. **去重 + 探索机制**：`t_UserRecv` 表记录"这个用户**看过**哪些视频"（v9 起：点击播放才写入，推送不写入），刷新后看过的视频不再出现；额外拿出3个坑位不看标签匹配、纯按热度推，保证用户不会被自己的标签"锁死"在同一类内容里。
4. **补位 + 换一批（v9 新增）**：没看过的视频不足 10 条时不留空白，用"最早看过的"（`recvTime` 升序）补满；`m_mapLastPushed` 记住上一批推送，下次刷新优先避开，实现"10 条都不喜欢 → 点刷新整批换掉"。

---

## 二、推荐主流程（代码逐段讲解）

### 第一步：`DownloadRq` 接到请求，做基础校验后转交 `GetFileList`

```cpp
void CLogic::DownloadRq(sock_fd clientfd, char *szbuf, int nlen)
{
    // 校验1：收到的字节数不够装下一个 STRU_DOWNLOAD_RQ 结构体，说明是非法/截断的包，直接丢弃
    if (nlen < sizeof(STRU_DOWNLOAD_RQ))
    {
        printf("非法包：长度不够，丢弃！nlen=%d\n", nlen);
        return;
    }
    // 校验2：指针判空，防止空指针解引用崩溃
    if (!szbuf)
    {
        printf("szbuf 为空！\n");
        return;
    }
    STRU_DOWNLOAD_RQ *rq = (STRU_DOWNLOAD_RQ*)szbuf;
    list<FileInfo*> fileList;

    // 核心推荐逻辑全在这一行里，GetFileList 把算好的推荐结果塞进 fileList
    GetFileList(fileList, rq->m_nUserId);

    // 后面是把 fileList 里每个视频的缩略图(GIF)按 64KB 满块变长分块发给客户端
    // 跟推荐策略本身无关，是文件传输逻辑，这里不展开
    ...
}
```

### 第二步：`GetFileList` 四级填坑（v9 当前版本）

```cpp
void CLogic::GetFileList(list<FileInfo*>& fileList, int userid)
{
    // 推荐策略（每次刷新最多10个坑位）：
    //   t_UserRecv 语义 = 【看过】（VideoClickRq 点击播放时写入 + recvTime）
    //   ① 优先推没看过、且不在"上一批推送"��的视频（7个按标签匹配+热度，3个纯热度防茧房）
    //   ② 没看过的不够10条 → 用没看过但上一批推过的补（"换一批"轮转）
    //   ③ 还不够 → 用看过的补位，最早看过的(recvTime最旧)优先 → 不留空白坑位
    //   ④ 全部视频都看完 → 清空看过记录，开启新一轮循环

    // ---- 统计还没看过的视频数（过滤条件必须与推荐查询一致：picName/picPath 非空）----
    // 注意：计数和推荐必须用同一套脏数据过滤，否则脏记录永远"看不完"，重置循环永远不触发
    "select count(videoId) from t_VideoInfo where picName <> '' and picPath <> '' "
    "and videoId not in (select videoId from t_UserRecv where userId = %d);"

    // ---- ④ 全部看完：清空"看过"记录，重开循环 ----
    if (nUnwatched == 0)
        "delete from t_UserRecv where userId = %d;"
    ...
```

**面试常问**："用户看完所有视频后怎么处理？" → 答：统计"未看过视频数"，为 0 就清空该用户的 `t_UserRecv`，让推荐重新从头开始循环，不会出现"无视频可推"的死局。

### 第三步：第①级——前 7 个坑位标签匹配 + 后 3 个坑位纯热度，都排除"看过的"和"上一批"

```cpp
// 前7坑位：没看过 + 不在上一批（lastPushedIds 来自内存 m_mapLastPushed），标签匹配度优先
sprintf(sqlBuf,
    "select v.videoId, v.picName, v.picPath, v.rtmp "
    "from t_VideoInfo v, t_UserData u "
    "where u.id = %d and v.picName <> '' and v.picPath <> '' "
    "and v.videoId not in (select videoId from t_UserRecv where userId = %d) "  // 排除看过的
    "and v.videoId not in (%s) "                                                 // 排除上一批（换一批）
    "order by "
    // 打分公式：用户标签(u.xxx=1) 且 视频标签(v.xxx=1) 都为1才计1分，8个维度逐个累加
    // 本质是两个8维"0/1向量"的点积，点积越大说明用户和视频的标签重合越多
    "((u.food=1 and v.food=1) + ... + (u.edu=1 and v.edu=1)) desc, "  // 一级排序：匹配分
    "v.hotdegree desc "                                                // 二级排序：热度
    "limit 0,7;", ...);
FetchVideos(sqlBuf, fileList, slotIndex, excludeIds, pickedIds);   // 统一解析+校验+去重

// 后3坑位：同样排除看过的+上一批，但完全不看标签、纯热度倒序（防信息茧房）
```

v9 起每条选中的视频**不再写 `t_UserRecv`**（推送≠看过），只在内存 `pickedIds` 里记录、最后整批存进 `m_mapLastPushed[userid]` 供下次"换一批"。

### 第四步：第②③级补位——保证凑满 10 个坑位不留空白

```cpp
// ② 没看过的不够10条：放开"上一批"限制，把上一批推过但没看的拿回来补
if (fileList.size() < TOTAL_SLOTS)
    ...  // 同①的查询，只是去掉 lastPushedIds 排除条件

// ③ 还不够：用看过的补位，最早看过的优先（LRU：recvTime 升序，NULL 视为最旧）
if (fileList.size() < TOTAL_SLOTS)
    "select v.videoId, v.picName, v.picPath, v.rtmp "
    "from t_VideoInfo v join t_UserRecv r on v.videoId = r.videoId and r.userId = %d "
    "where v.picName <> '' and v.picPath <> '' and v.videoId not in (%s) "
    "order by r.recvTime asc "     // 最早看过的排最前
    "limit 0,%d;"
```

**为什么③按 `recvTime` 升序**：配合 `VideoClickRq` 的"先删后插刷新 recvTime"（LRU 语义），重看一个视频它就挪到"最近"，下次补位轮到别的老视频——补位也是轮转的，不会每次都补同几个。

**为什么要特意留 3 个"不看标签"的坑位**：如果 10 条推荐全按标签匹配度排，用户会永远只刷到自己勾选过的那几类内容，刷不到新东西，这就是推荐系统里经典的"信息茧房/过滤气泡（Filter Bubble）"问题。留 3 个纯热度坑位，相当于人为注入随机性/多样性——这是推荐系统里"探索与利用（Exploration vs Exploitation）"权衡的一个朴素实现。

### 第五步：`FetchVideos` 辅助函数（v9 抽取）

原来前7/后3坑位是两段近乎复制的 90 行解析循环，v9 抽成一个函数统一做四件事：解析 SQL 结果 → **批内去重**（`pickedIds.count()`，防 t_UserRecv 历史重复行让同一视频占多个坑位）→ 缩略图文件非空校验（fopen+ftell，空文件/打不开的脏数据跳过）→ 记录 videoId 到排除集。四级填坑共用，判空逻辑只写一处。

---

## 三、热度（hotdegree）现在是单源驱动：只有播放上报真正改写它

历史上热度由"曝光 / 点击 / 播放"三级信号共同驱动，但联调时发现两个注水 bug（见变更记录 v6/v7），已先后把曝光和点击从热度计算中移除。**当前版本 hotdegree 只由播放上报（PlayReportRq）单源驱动**：每次播放上报从 `t_VideoPlayLog` 近30天明细单条 SQL 同时派生 `playCount` 与 `hotdegree`，数据同源、不再漂移。

| 信号 | 触发点 | 当前是否影响 hotdegree | 说明 |
|---|---|---|---|
| 曝光 | `GetFileList` 把视频选入推荐列表时 | **否（v6 删除）** | 仅做去重入表（t_UserRecv），不再加分 |
| 点击 | 客户端双击卡片、准备播放的瞬间（`VideoClickRq`） | **否（v7 删除）** | 仅记录点击事件日志，不再改 hotdegree |
| 播放行为 | 播放结束/切视频/关窗口时上报观看时长和完播率（`PlayReportRq`） | **是（唯一入口）** | 最强信号，按"近30天完播率中枢惩罚式"重算，同时派生 playCount |

### 3.1 曝光（已移除于 v6）

v6 之前 `GetFileList` 每把一条视频推入列表就执行 `update t_VideoInfo set hotdegree = hotdegree + 1`。联调发现严重 bug：**"播一个视频，其他被推荐过的视频热度也跟着涨"**——根因是原来的 `nCount==0` 清空去重表逻辑会让一大批视频被重新曝光、各自 `+1`。v6 把"清空去重表"改成"兜底模式（全部推完直接按热度展示全部、不再清表），并删除了曝光处的 `hotdegree+1`。

现在曝光只负责把 `(userId, videoId)` 写入 `t_UserRecv` 做去重，完全不碰 hotdegree。这样也顺带消除了马太效应（曝光不再给热度输血）。

### 3.2 点击（v7 起不再改热度；v9 起改为写"看过"表）

v7 之前 `VideoClickRq` 双击卡片瞬间就 `update t_VideoInfo set hotdegree = hotdegree + 5`。联调又发现第二个 bug：**"没播放热度也涨"**——双击卡片就会加分，哪怕用户看都没看。v7 把这一行删除。

v9 起 `VideoClickRq` 有了新职责：**写 `t_UserRecv`（"看过"记录）**——点击播放的瞬间就是"看过"的判定时刻。用"先删后插"刷新 `recvTime`，实现 LRU 语义（重看会把该视频挪到"最近看过"，影响补位轮转次序）：

```cpp
// ===== 视频被点击播放（纯上报，不回复） =====
void CLogic::VideoClickRq(sock_fd clientfd, char* szbuf, int nlen)
{
    STRU_VIDEO_CLICK_RQ* rq = (STRU_VIDEO_CLICK_RQ*)szbuf;
    if (rq->m_nUserId <= 0 || rq->m_nVideoId <= 0) return;

    // t_UserRecv 语义改为【看过】：点击播放才写表。
    // 先删后插以刷新 recvTime（LRU）：重看一个视频会把它挪到"最近看过"，
    // 补位推荐时按 recvTime 升序取"最早看过的"优先推
    sprintf(sqlBuf, "DELETE FROM t_UserRecv WHERE userId = %d AND videoId = %d;", ...);
    sprintf(sqlBuf, "INSERT INTO t_UserRecv(userId, videoId, recvTime) VALUES(%d, %d, NOW());", ...);

    // 热度仍只由 PlayReportRq（真实播放+完播率）决定，此处不写 hotdegree
}
```

客户端发送逻辑（`OnlineDialog::reportVideoClick`）保持不变——双击卡片依旧发 `VideoClickRq`。注意：点击 **hotdegree 加 0** 但会**写"看过"记录**（影响推荐去重）；用户如果真的开始播放并在结束后上报（切视频/放完/关窗口），`PlayReportRq` 才会按完播率给该视频加热度；只点开几秒就关、完播率 ≤0.3 的视频，热度会被重算结果钳到 0（见 3.3）。

> 依赖的表结构变更：`ALTER TABLE t_UserRecv ADD COLUMN recvTime DATETIME DEFAULT NULL;`（旧行 recvTime 为 NULL，排序时视为"最旧"，正好符合补位语义）。

### 3.3 播放上报重算：最强信号，也是踩过坑、修好之后才真正生效的一个

```cpp
// ===== 播放时长/完播率上报（纯上报，不回复） =====
void CLogic::PlayReportRq(sock_fd clientfd, char* szbuf, int nlen)
{
    STRU_PLAY_REPORT_RQ* rq = (STRU_PLAY_REPORT_RQ*)szbuf;

    // 1. 落一条明细记录：谁、看了哪个视频、实际看了多少秒、视频总共多少秒
    char sqlBuf[_DEF_SQLIEN] = "";
    sprintf(sqlBuf,
        "INSERT INTO t_VideoPlayLog (videoId, userId, watchSeconds, totalSeconds, playTime) "
        "VALUES (%d, %d, %d, %d, NOW());",
        rq->m_nVideoId, rq->m_nUserId, rq->m_nWatchSeconds, rq->m_nTotalSeconds);
    m_sql->UpdataMysql(sqlBuf);

    // 2. 以 t_VideoPlayLog 为唯一数据源：playCount 与 hotdegree 在同一条 SQL 内一起从日志派生，
    //    不再用独立的 "SET playCount = playCount + 1"，消除两套计数器漂移。
    //    playCountAll=历史总播放次数；playCount30d=近30天有效播放；avgCompletion30d=近30天平均完播率
    //    hotdegree 公式（中枢惩罚式）：playCount30d * 25 * (avgCompletion30d - 0.3)
    //      完播率>0.3 才加分(权重25/次)；<=0.3 算负被 GREATEST(0,...) 托底为0；热度恒非负。
    char hotSqlBuf[800] = "";
    sprintf(hotSqlBuf,
        "UPDATE t_VideoInfo v LEFT JOIN ("
        "SELECT videoId, "
        "COUNT(*) AS playCountAll, "
        "COUNT(CASE WHEN playTime > NOW() - INTERVAL 30 DAY AND totalSeconds>0 THEN 1 END) AS playCount30d, "
        "AVG(CASE WHEN playTime > NOW() - INTERVAL 30 DAY AND totalSeconds>0 THEN watchSeconds/totalSeconds END) AS avgCompletion30d "
        "FROM t_VideoPlayLog WHERE videoId=%d "
        "GROUP BY videoId) p ON p.videoId=v.videoId "
        "SET v.playCount = IFNULL(p.playCountAll, 0), "
        "v.hotdegree = GREATEST(0, ROUND(IFNULL(p.playCount30d,0) * 25 * (IFNULL(p.avgCompletion30d,0) - 0.3))) "
        "WHERE v.videoId=%d;",
        rq->m_nVideoId, rq->m_nVideoId);
    m_sql->UpdataMysql(hotSqlBuf);
}
```

**这里是踩过坑、后来定位到根因的地方，面试可以拿来讲排查过程**：`STRU_PLAY_REPORT_RQ` 结构体的构造函数只有 `memset(this, 0, sizeof(*this))`，不会自动把 `m_nType` 设成 `_DEF_PACK_PLAY_REPORT_RQ`；而客户端发送这个包（`playerdialog.cpp::reportPlaybackIfNeeded`）之前一直没有手动补上 `rq.m_nType = _DEF_PACK_PLAY_REPORT_RQ;` 这一行，导致发出去的包类型恒为 0。服务端 `TCPKernel.cpp::DealData` 的分发逻辑：

```cpp
PackType type = *(PackType*)szbuf;
if( (type >= _DEF_PACK_BASE) && ( type < _DEF_PACK_BASE + _DEF_PACK_COUNT) )
{
    PFUN pf = NetPackMap( type );
    if( pf ) (TcpKernel::GetInstance()->m_logic->*pf)( clientfd , szbuf , nlen);
}
```

包类型是 0，`_DEF_PACK_BASE` 是 10000，`0 >= 10000` 为假，直接被这行判断挡住，连 `NetPackMap` 都没进，更不会调用到 `PlayReportRq`——服务端连收都没收到（准确说是收到了 TCP 数据，但业务层直接丢弃），日志自然什么都不会打印。这也是为什么之前无论怎么改触发时机、检查登录状态、重新编译部署，这条链路都毫无反应的真正原因，跟那些环节都没关系。现在补上 `rq.m_nType=_DEF_PACK_PLAY_REPORT_RQ;` 之后，这条链路才第一次真正被打通。

（对照一下 `livedialog.cpp` 里发直播请求包的写法，那边是显式写了 `rq.m_nType = _DEF_PACK_LIVE_START_RQ;`，说明这个"构造函数不设置类型，调用方必须手动补"是项目里既有的写法约定，只是播放上报这里漏写了，是个很容易被忽略的隐蔽 bug——编译不会报错，运行也不会崩溃，纯粹是"看起来什么都没发生"，比逻辑错误更难排查。）

**时间衰减**：只统计 `WHERE playTime > NOW() - INTERVAL 30 DAY` 这个窗口内的数据，超过30天的播放记录自动不再计入，不需要指数衰减公式，靠"滑动窗口"天然实现。

### 3.4 现在只有"播放上报单源"在改写 hotdegree

- 点击写"看过"表（`t_UserRecv` + recvTime，v9），**不改 hotdegree**；推送（曝光）什么表都不写（v9 起推送不写 t_UserRecv）。
- hotdegree 唯一由 `PlayReportRq` 的"整体重新计算并覆盖"驱动（`hotdegree = GREATEST(0, ROUND(...))`）。每次播放上报都从 `t_VideoPlayLog` 整段重算，**数据同源、不再漂移**（v8 把原来独立的 `playCount = playCount + 1` 累加也合并进同一条 SQL）。这条链路在 v5 修好包类型漏设之后才真正跑通，所以现在的 hotdegree 完全由"真实播放 + 完播率"决定，是最可信的信号。

### 3.5 离线维护工具：`tools/recompute_hotdegree.sql`（非必需）

不再是热度更新的必经路径，只在想一次性把全站视频的热度按最新公式重刷一遍时手动执行：

```bash
mysql -u root -p123456 -D MediaServer < /path/to/recompute_hotdegree.sql
```

注意两点一致性：
1. 脚本里的 hotdegree 公式要和 `PlayReportRq` 内嵌的 **中枢惩罚式** 公式一致：`GREATEST(0, ROUND(playCount30d * 25 * (avgCompletion30d - 0.3)))`。
2. v8 之后 playCount 也由这条日志派生，**离线脚本必须同时重算 `playCount = playCountAll`**，否则只刷 hotdegree 不刷 playCount 会造成"两套计数器"再次漂移（这正是 v8 想根除的问题）。

### 3.6 热度被用在哪里

- `GetFileList` 前 7 坑位：标签匹配分打平后，按 `hotdegree desc` 做二级排序
- `GetFileList` 后 3 坑位：完全不看标签，直接 `hotdegree desc` 取前 3
- `UploadHistoryRq`（上传历史页）：`order by hotdegree desc` 展示用户自己上传的视频，热度高的排前面

### 3.7 新视频冷启动

新视频上传入库时 `hotdegree` 显式设为 `0`、`playCount` 也为 `0`，之后只有被**真实播放并上报**（切视频/放完/关窗口触发 `PlayReportRq`）时才会按近30天完播率累积热度；曝光和点击都不加分（v6/v7 已移除）。在还没有任何播放数据之前，完全靠"标签匹配"这一条路径获得曝光机会——这也是为什么前7坑位用标签匹配度优先、后3坑位用热度兜底的组合对新视频比较友好。

---

## 四、推荐系统术语对照（面试时要能对上号）

| 本项目怎么做的 | 对应的推荐系统概念 |
|---|---|
| 用户标签 × 视频标签匹配打分 | 基于内容的推荐（Content-based Filtering） |
| 8维0/1标签向量做"点积"打分 | 向量相似度计算的一种最简化形式（比余弦相似度、Jaccard相似度更粗糙） |
| `hotdegree` 排序 | 基于热度的推荐（Popularity-based Recommendation），常用于冷启动兜底 |
| 后3坑位纯热度、不看标签 | 探索-利用权衡（Exploration vs Exploitation）、缓解信息茧房（Filter Bubble） |
| `t_UserRecv` "看过"表（v9） | 已消费过滤（Consumed-item Filtering），比"已曝光过滤"更弱的排除条件——曝光未消费的内容仍可再推 |
| 全部看完后清空重置 | 简化版的"推荐池循环利用" |
| `m_mapLastPushed` 换一批（v9） | 批次轮转/曝光打散（Impression Discounting 的朴素形式） |
| 看过的按 recvTime 最早优先补位（v9） | LRU 重推——"最久没看的最值得重推" |
| 点击/播放两级信号 | 隐式反馈（Implicit Feedback）的分级建模——点击驱动"看过"去重，播放驱动热度 |

**没有做的**（面试如果问到要如实说没做）：
- **协同过滤（Collaborative Filtering）**：没有用到"用户-物品交互矩阵"，纯粹是内容维度的标签匹配。
- **机器学习模型**：没有模型训练/在线学习，hotdegree 公式（`playCount30d * 25 * (avgCompletion30d - 0.3)`，阈值0.3/权重25）是写死的经验值，没做过数据驱动调参。
- **显式反馈（点赞等主动打分）**：做过一版点赞功能，因跑不通已回退，现在的三级信号都是隐式行为（曝光/点击/播放），不吃用户主动打分类的显式反馈。

---

## 五、面试大概率会问的问题（含参考答案）

### Q1：介绍一下你的推荐算法/策略
> "我实现的是一个规则驱动的混合推荐：用户注册时勾选8个兴趣标签，视频上传时也带同样的8个标签，推荐时按'匹配标签数'给视频打分排序，取前7个坑位；同时留3个坑位完全不看标签、纯按全站热度推，避免用户被自己的标签锁死在单一类内容里。去重语义是'看过才排除'——点击播放的瞬间写 `t_UserRecv`（带 recvTime），刷新后看过的视频不再出现；没看过的不足10条时用'最早看过的'补位（LRU 轮转），不留空白坑位；内存里还记着上一批推送的10条，下次刷新优先避开，实现'一条都不喜欢就点刷新换一批'；全部看完则清空记录重开循环。热度（hotdegree）由播放上报单源驱动——每次播放结束/切视频/关窗口时上报观看时长和完播率，`PlayReportRq` 从 `t_VideoPlayLog` 近30天明细用'中枢惩罚式'公式重算（`playCount30d * 25 * (avgCompletion30d - 0.3)`，完播率>0.3才加分、否则钳到0），曝光和点击都不加热度。"

### Q2：这是协同过滤吗？
不是。协同过滤依赖"用户-物品交互矩阵"，通过"相似用户喜欢什么"或"相似物品被谁喜欢"来推荐；本项目是直接拿用户自己的标签和视频自己的标签做匹配，是**基于内容的推荐**，两者完全不是一回事，能清楚区分这两个概念本身就是加分项。

### Q3：怎么解决冷启动问题？（新用户没有行为历史 / 新视频没有热度）
- **新用户冷启动**：本项目天然规避了这个问题——用户是注册时**主动勾选**兴趣标签，不需要靠历史行为积累画像，标签从第一秒就有，能直接参与匹配排序。
- **新视频冷启动**：新视频 `hotdegree`/`playCount` 入库时都是 0，只有被真实播放并上报后才会按完播率累积热度；标签匹配优先于热度排序，能帮助新视频获得初始曝光。但如果标签也不匹配、又没积累到播放数据，确实很难被推荐到——可以诚实说明，并提出"给新视频一个短期热度加成/流量扶持"作为改进方向。

### Q4：`hotdegree`（热度）是怎么算的？这个设计经历过哪些迭代？
**这是最容易被追问出深度的一个点，建议主动讲，能完整讲出"设计缺陷→改进→踩坑→定位根因→修复"这条完整的演进和排查过程，是很有说服力的加分项**：

- **v1（曝光即加分）**：视频只要被推荐推送就 `+1`，跟用户是否真的感兴趣无关，有马太效应问题。
- **v2（定时批处理）**：改成客户端上报播放时长/完播率，服务器 crontab 定时批量重算。解决了 v1 的偏差问题，但部署链路长（脚本路径、crontab 配置、版本同步），任何一环出错都很难排查。
- **v3（事件实时重算）**：把重算逻辑直接内嵌进播放上报的处理函数，去掉定时任务依赖。
- **v4（曝光+1 保底）**：v3 代码没问题，但联调时播放上报这条链路一直"发了没反应"，热度长期是0，为了保证功能有可见效果，先把曝光+1加回来兜底。
- **v5（定位到真正根因并修复）**：往下查发现，播放上报的请求结构体 `STRU_PLAY_REPORT_RQ` 构造函数只做了 `memset` 清零，客户端发送前忘了手动设置包类型字段，导致服务端网络层收到包但类型校验不通过、直接静默丢弃，根本没进业务逻辑——这跟登录状态、触发时机、部署环节都没关系，就是一行赋值语句漏写了。补上之后播放上报链路才第一次真正跑通。同时又加了一个更简单可靠的"点击播放+5"信号（双击卡片瞬间同步触发，链路比播放上报短很多），形成曝光/点击/播放上报三级权重并存。
- **v6（去掉曝光注水）**：联调发现"播一个视频、其他被推荐过的视频热度也跟着涨"——根因是 `GetFileList` 在 `nCount==0` 清空去重表后重新推送，导致大批视频被重新曝光、各自+1。改成"全部推完进入兜底展示模式（不清表）"并删除曝光处的 `hotdegree+1`。
- **v7（去掉点击注水）**：又发现"没播放热度也涨"——双击卡片就 `hotdegree+5`。删除这一行，`VideoClickRq` 只记录点击、不再改 hotdegree。至此曝光与点击彻底退出热度计算。
- **v8（消除 playCount 双计数器漂移 + 中枢惩罚式公式，当前版本）**：原来 `PlayReportRq` 里既有独立 `playCount = playCount + 1` 累加、又有覆盖式重算 hotdegree，两套计数器会漂移（上报丢失时 playCount 漏加但 hotdegree 照算）。改成单条 SQL 从 `t_VideoPlayLog` 同时派生 `playCount`（全量）和 `hotdegree`，且 hotdegree 改用中枢惩罚式 `GREATEST(0, ROUND(playCount30d * 25 * (avgCompletion30d - 0.3)))`——完播率>0.3 才加分（权重25/次），≤0.3（看一眼就关）整体算负被钳到0，实现"不感兴趣则热度不涨/清零"，热度恒非负。

（如果面试官追问"这种丢包为什么编译期和运行期都没报错"：因为这是纯粹的业务语义错误，不是内存越界或类型不匹配，C++编译器和运行时都没有能力知道"这个int字段该被赋成哪个值"，只能靠约定+人工检查，或者用更安全的设计比如让struct的默认构造函数直接把类型硬编码好（不给调用方留下"忘记赋值"的空子），这是这次踩坑之后可以做的改进——把 `PlayType` 的设置从"调用方手动赋值"改成"构造函数里写死"，从设计上消除这一类问题。）

### Q5：为什么打分公式直接写在 SQL 的 `ORDER BY` 里，会有什么问题？
```sql
order by ((u.food=1 and v.food=1) + ... ) desc, v.hotdegree desc
```
这种写法**没法用索引加速**，MySQL 必须把候选行取出来后逐行现算 8 次布尔运算再排序，数据量小的时候没问题，视频/用户规模变大后会有明显的排序开销。改进方向：把匹配分离线预计算好存成一列（或者引入专门的推荐/搜索引擎如 Elasticsearch），而不是每次请求都现场算。

### Q6：怎么保证不给用户重复推荐同一个视频？
去重分三层（v9）：
1. **跨刷新去重（看过的）**：`t_UserRecv(userId, videoId, recvTime)` 记录"看过"（点击播放时写入），推荐查询用 `NOT IN` 排除；
2. **跨批次轮转（没看但推过的）**：内存 `m_mapLastPushed` 记住上一批推送的 10 条，下次刷新第①级优先避开——不点任何视频连刷两次，看到的也是不同批次（视频总量>10时）；
3. **批内去重**：`excludeIds` + `pickedIds`（`FetchVideos` 内检查）防止四级填坑之间同一视频被选中两次——尤其第③级 join t_UserRecv 时历史重复行会让同一视频返回多次，靠 `pickedIds.count()` 拦住。

### Q7：并发场景下这套逻辑有没有问题？
**如果被问到，如实承认**：`GetFileList` 对同一个 `userid` 没有做互斥保护，同一用户几乎同时发起两次 `DownloadRq`，两次填坑会基于同一份"看过"快照计算，结果可能重叠（`m_mapLastPushed` 本身有 `m_recMutex` 保护，不会写坏，但语义上是"后写覆盖"）。`VideoClickRq` 的"先删后插"两条 SQL 不是原子的，并发点击同一视频理论上可产生重复行——但 `FetchVideos` 的批内去重兜住了它对推荐结果的影响。改进方向：给 `t_UserRecv` 加 `(userId, videoId)` 联合唯一索引 + `INSERT ... ON DUPLICATE KEY UPDATE recvTime=NOW()` 一条 SQL 原子完成。热度更新入口现在只有 `PlayReportRq`（播放上报）一个，它是幂等的"重新计算并覆盖"，并发下不会导致数据错误，最多是有一点锁等待的性能损耗。

### Q8：为什么是"7+3"这个比例，不是别的数字？
这是一个写死的经验值，没有做 A/B 测试或数据驱动的调优，本质是"给内容多样性预留 30% 曝光位"的朴素设计。如果被问"怎么得出这个比例"，可以诚实说是经验设定，并提出改进方向：比如根据用户历史点击率动态调整探索坑位的比例，这是更成熟推荐系统会做的"动态Exploration比例"思路。

### Q9：hotdegree 公式里的阈值 0.3、权重 25 是怎么定的？
都是拍脑袋定的经验值，没有做过数据驱动的调参，可以如实说明。相对关系是刻意设计的：以完播率 0.3 为"中枢"，高于它（认真看了）才加分、且越接近看完加得越多，低于它（看一眼就关＝不感兴趣）则重算结果被钳到 0、等于热度不涨/清零，权重 25 决定每次有效播放的加分幅度。可以主动指出两个改进方向：(1) 阈值/权重应靠数据（如 A/B 实验）标定而非凭直觉；(2) 完播率用算术平均在样本量很小时（如只播放过1次、完播率恰好100%）容易被单条极端记录拉高，缺乏"样本量越少越不可信"的置信度修正（比如贝叶斯平均）。

---

## 六、涉及代码位置速查

- `MediaServer-live/MediaServer/src/clogic.cpp`
  - `DownloadRq`：入口，做包体合法性校验后调用 `GetFileList`，然后按 64KB 满块变长发送 GIF 缩略图
  - `GetFileList`：核心推荐逻辑（v9 四级填坑）：①没看过+不在上一批（7标签+3热度）→②没看过但上一批推过→③看过的按 recvTime 最早补位→④全看完清表重开循环
  - `FetchVideos`：v9 抽取的辅助函数——解析推荐 SQL 结果 + 批内去重 + 缩略图非空校验
  - `UploadHistoryRq`：上传历史页，按 `hotdegree desc` 排序（注意：回复头复用 STRU_DOWNLOAD_RS 但必须显式设 `m_nType = _DEF_PACK_UPLOADHISTORY_RS`，构造函数默认是 DOWNLOAD_RS——踩过的坑，同 PlayReport 包类型漏设一个性质）
  - `PlayReportRq`：播放时长/完播率上报入口，写 `t_VideoPlayLog` 明细 + 单条 SQL 同时派生 `playCount` 与 `hotdegree`（中枢惩罚式，唯一热度入口）
  - `VideoClickRq`：点击播放上报——v9 起写 `t_UserRecv`"看过"记录（先删后插刷新 recvTime，LRU），不改 hotdegree
- `MediaServer-live/MediaServer/include/clogic.h`：`m_mapLastPushed`（换一批的上一批记忆，`m_recMutex` 保护）
- `MediaPlay-live/MediaPlay/playerdialog.cpp::reportPlaybackIfNeeded`：客户端发送播放上报的地方，`rq.m_nType` 需要手动设置（踩过的坑）
- `MediaPlay-live/MediaPlay/onlinedialog.cpp::reportVideoClick`：客户端发送点击上报的地方（`slot_PlayClicked` / `slot_MyPlayClicked` 里触发，空坑位 videoId<=0 不上报）
- `MediaPlay-live/MediaPlay/onlinedialog.cpp::on_pb_fresh_clicked`：刷新按钮——请求前先 `clear()` 15 张卡片，防止新一批不足10条时旧卡片残留
- `MediaServer-live/MediaServer/src/TCPKernel.cpp::DealData`：网络层包分发入口，含按包类型的最小长度校验表（`GetMinPackLen`）；`type >= _DEF_PACK_BASE` 校验漏包类型会被这里静默挡住
- `MediaServer-live/MediaServer/tools/recompute_hotdegree.sql`：离线维护工具，非热度更新必经路径，仅用于手动批量重刷
- 涉及表：
  - `t_UserData`：存用户的8个兴趣标签（注册时填写）
  - `t_VideoInfo`：存视频的8个标签 + `hotdegree` 热度字段 + `playCount` 播放计数
  - `t_UserRecv(userId, videoId, recvTime)`：**"看过"记录表**（v9 语义变更：点击播放才写入；recvTime 供 LRU 补位排序，需执行 `ALTER TABLE t_UserRecv ADD COLUMN recvTime DATETIME DEFAULT NULL;`）
  - `t_VideoPlayLog(videoId, userId, watchSeconds, totalSeconds, playTime)`：播放行为明细表，是播放上报重算的数据来源
