# 断点续传 v2 到底干了啥（人话版）

> 这份是给"不想看技术黑话"的人写的。先说人话,再给带注释的代码。
> 配套文件都在 `D:\newproject\`：`mediaserver_busy_fix.patch`、`orphan_cleanup.patch`、`orphan_cleanup.md`、`stress_test_client.cpp`。

---

## 一、一句话总结

我们干了两件事：**把视频上传改成了"断点续传"（传一半断了能接着传）**，顺便**修了一个卡死 bug**，还**加了个自动清理功能（删掉那些传一半又放弃的垃圾文件）**。

---

## 二、这次一共做了几件事

| 事 | 是啥 | 结果 |
|----|------|------|
| 修 bug | 上传传到一半断了，再传就卡死（报 busy） | ✅ 修好了，测试通过 |
| 临时调试 | 为了手动测试加的限速，测完撤掉了 | ✅ 已撤回，干净 |
| 新功能 | 自动清理"传一半放弃的视频" | ✅ 写好并验证通过 |
| 决定不做 | 秒传时往数据库写一条记录 | ❌ 你说不做了 |
| 部署 | 把代码编译放到 Linux 上跑 | ✅ 跑起来了 |
| 验证 | 故意造假文件，看清理功能会不会乱删 | ✅ 没乱删，通过 |

---

## 三、断点续传到底是啥（用大白话讲）

**场景**：你传一个 100MB 的视频，传到 50MB 时网络断了 / 你手滑关了客户端。

- **以前**：只能从头再传 100MB，前面 50MB 白传了。
- **现在**：再传的时候，服务端说"你那 50MB 我收着呢，从第 50MB 接着发就行"。这就叫断点续传。

实现上靠 4 个东西：

1. **MD5（文件的身份证）**：上传前先算整个文件的 MD5（一串 32 位字符）。服务端拿这个 MD5 认出"这是同一个文件"，而不是靠随机编号（以前就是靠随机编号，所以认不出续传）。
2. **taskId（服务端发的任务号）**：握手时服务端分配一个号，之后每传一块都带着这个号，服务端才知道"这块是哪个文件的"。
3. **64KB 一块 + 记录位置**：文件切成 64KB 的小块传。每收一块，服务端记下"收到第几字节了"。断了再连，服务端告诉客户端"从第 X 字节接着发"。
4. **两个临时文件 `.part` 和 `.meta`**：
   - `xxx.part` = 目前收到的那一半字节（真正的半截视频）
   - `xxx.meta` = 进度条（md5、已收多少、叫啥名）。每收一块就更新一下。
   - 等全收完、校验没问题，才把 `.part` 改名为正式视频文件。

---

## 四、修的 bug：传到一半断了再传就卡死（busy）

### 现象
测试时发现：传 50% → 模拟断网 → 再传，服务端回 `busy(4)`，死活不让续传。要等 30 分钟才自动解锁。

### 原因（大白话）
服务端有个"占用锁"：同一个用户传同一个文件时，加把锁，防止两个人同时传冲突。
正常传完会"还锁"。但**如果客户端是异常断网（没说"我传完了"就直接关了）**，服务端只关了 socket，**忘了还这把锁**。锁就一直挂着 → 下次同一用户传同一个文件，一看"锁还挂着" → 以为有人在传 → 报 busy。

**根子上的问题**：负责网络的模块（管 socket 的那个）和负责业务的模块（管上传逻辑的）是分开的。网络模块发现"断线了"，但它手里没有业务模块的电话，不知道该通知业务模块"去还锁"。所以锁就漏还了。

### 怎么修的
给网络模块配了个"业务模块的电话"（`m_pLogic`），断线时打这个电话通知业务模块去还锁。

下面是修好的核心函数（每行有注释）：

```cpp
// 功能：客户端异常断线时（没发"传完了"就关了 socket），清理它占着的资源
// 重要：只清"锁"和"任务记录"，【不删】.part/.meta 文件，
//       因为客户端可能等会儿回来接着传（断点续传就靠保留这两个文件）
void CLogic::OnClientDisconnect(sock_fd clientfd)
{
    std::lock_guard<std::mutex> lk(m_v2Mutex);  // 加锁，防止多线程同时改任务表
    // 遍历所有正在传的任务，找出"属于这个断线连接"的
    for (auto it = m_mapTaskIdToFileInfo.begin(); it != m_mapTaskIdToFileInfo.end(); ) {
        FileInfo* info = it->second;
        if (info && info->m_clientfd == clientfd) {   // 是这个断线连接的任务
            if (info->pFile) { fclose(info->pFile); info->pFile = NULL; }  // 关掉文件句柄
            char busyKey[160];
            snprintf(busyKey, sizeof(busyKey), "%d_%s", info->m_nUserId, info->m_szMd5);
            m_mapBusy.erase(busyKey);   // ★ 关键：把"占用锁"删掉，下次传就不会再报 busy(4)
            delete info;                 // 释放这块内存
            it = m_mapTaskIdToFileInfo.erase(it);  // 从任务表移除
        } else {
            ++it;
        }
    }
    // 注意：这里【没有】remove(.part/.meta)，留着给客户端续传
}
```

网络模块那边，断线时多打一个电话：

```cpp
    ev->eventdel();              // 从监听列表摘掉这个 socket
    close(ev->fd);               // 关掉连接
    // ★ 关键：通知业务层"这个连接断了"，让它清掉占用锁和任务记录
    //   （之前这里只 close、忘了通知业务层，导致 busy(4) 卡死 bug）
    if (pthis->m_pLogic) pthis->m_pLogic->OnClientDisconnect(ev->fd);
```

### 修的时候顺带踩的 3 个小坑（编译报错）
1. `sock_fd` 类型找不到 → 因为底层公共头文件不能反向引用另一个头文件。解决：字段改成 `int`（跟 `sock_fd` 是一回事）。
2. 第 1 个坑连锁导致构造函数报错 → 跟着第 1 个一起解决了。
3. 那个"电话"变量 `m_pLogic` 放在了私有区，外面赋不进去 → 挪到公有区。

---

## 五、临时调试钩子（已撤回）

为了让你能手动测续传，我临时在**客户端**加了俩调试开关：一个限速（让你有时间手动关客户端）、一个把块改小（传得慢）。
测完你让我改回来，已经撤了，客户端现在是干净的生产状态。**服务端没动这两处**（块大小是协议常量，改它要两端一起重编，没必要）。

---

## 六、新功能：自动清理"传一半放弃的视频"（孤儿）

### 啥是孤儿
就是你传视频传到一半，**然后放弃了、再也没回来续传**。那个半截 `.part` 和 `.meta` 就永远躺硬盘上占地方，没人要了。这种就叫"孤儿文件"。

> 注意：正在传、还会回来续的，不叫孤儿，叫"续传点"，要留着。孤儿 = 被放弃的半截视频。

### 怎么判断哪个是孤儿（看"最后修改时间"）
关键前提：每收一块，`.part` 和 `.meta` 的"最后修改时间"都会被刷新。所以**最后修改时间 = 这个文件最后一次活跃的时间**。
我们规定：**超过 7 天没动静 = 客户端早放弃了 = 孤儿，删掉。**

### 三道保险（防误删真视频，这是设计第一原则）
1. **只认特定后缀**：扫描时只处理 `.part` 和 `.meta`，绝不碰 `.flv`（正式视频）。
2. **成对删**：删 `.meta` 时顺手把同名的 `.part` 也删了（反之亦然），不留"半个孤儿"。
3. **先演习后真删**：有个开关 `ORPHAN_DRYRUN`，默认开（只报告"会删哪些"，不真删）。人工确认判断对了，再关掉开关真删。

### 核心代码（带注释）

```cpp
// 启动扫描：清理超过 7 天没动静的孤儿 .part/.meta
// 时机：服务端一启动就跑一次（进程启动 = 构造函数里调用）
void CLogic::CleanupOrphanTempFiles()
{
    char root[1024];
    snprintf(root, sizeof(root), "%sflv/", RootPath);   // 视频根目录 /home/colin/video/flv/

    DIR* dUser = opendir(root);              // 打开根目录
    if (!dUser) { /* 打不开就跳过，不崩 */ return; }

    // 打印启动信息（DRY-RUN 表示"只演习不真删"）
    printf("[ORPHAN-CLEAN] start, dir=%s, threshold=%d days%s\n",
           root, ORPHAN_TTL_DAYS, ORPHAN_DRYRUN ? " (DRY-RUN)" : "");

    time_t now = time(NULL);                          // 当前时间
    long long ttl = (long long)ORPHAN_TTL_DAYS * 24 * 3600;  // 7天 = 多少秒
    int candidates = 0, stale = 0, kept = 0, removed = 0;    // 计数器

    struct dirent* de;
    while ((de = readdir(dUser)) != NULL) {           // 遍历每个用户目录
        if (de->d_name[0] == '.') continue;           // 跳过 . 和 .. 这两个特殊目录
        char userDir[2048];
        snprintf(userDir, sizeof(userDir), "%s%s/", root, de->d_name);

        DIR* dFile = opendir(userDir);                // 打开这个用户的目录
        if (!dFile) continue;

        struct dirent* fe;
        while ((fe = readdir(dFile)) != NULL) {       // 遍历目录里每个文件
            const char* name = fe->d_name;
            size_t nl = strlen(name);
            // 只看 .meta 和 .part 结尾的文件（白名单，防误删 .flv）
            bool isMeta = (nl > 5 && strcmp(name + nl - 5, ".meta") == 0);
            bool isPart = (nl > 5 && strcmp(name + nl - 5, ".part") == 0);
            if (!isMeta && !isPart) continue;         // 不是这俩后缀，直接跳过

            char full[2048];
            snprintf(full, sizeof(full), "%s%s", userDir, name);

            struct stat st;
            if (stat(full, &st) != 0) continue;       // 取文件信息失败就跳过

            ++candidates;
            long long age = (long long)now - (long long)st.st_mtime;  // 多久没动了（秒）
            if (age > ttl) {                          // 超过 7 天 = 孤儿
                ++stale;
                printf("[ORPHAN-CLEAN] stale(%s, age=%lld d): %s\n",
                       isMeta ? "meta" : "part", age / 86400, full);
                TryRemoveSibling(full, isMeta);       // 顺手删掉同名的兄弟文件（.meta↔.part）
                if (!ORPHAN_DRYRUN && remove(full) == 0) ++removed;  // 演习模式就不真删
            } else {
                ++kept;                               // 没超期，留着
            }
        }
        closedir(dFile);
    }
    closedir(dUser);

    // 打印总结：扫了多少、几个孤儿、留了几个、删了几个
    printf("[ORPHAN-CLEAN] done. candidates=%d, stale=%d, kept=%d, removed=%d%s\n",
           candidates, stale, kept, removed,
           ORPHAN_DRYRUN ? " (DRY-RUN, nothing deleted)" : "");
}

// 删掉 path 的同名"兄弟"文件（.meta 的兄弟是 .part，反之亦然）
static void TryRemoveSibling(const char* path, bool isMeta)
{
    std::string s(path);
    if (s.size() <= 5) return;
    s.resize(s.size() - 5);                        // 去掉末尾的 ".meta" 或 ".part"
    s += (isMeta ? ".part" : ".meta");             // 换成另一个后缀
    if (access(s.c_str(), F_OK) == 0) {            // 兄弟文件确实存在
        if (ORPHAN_DRYRUN) {
            printf("[ORPHAN-CLEAN]   (dry-run) sibling would be removed: %s\n", s.c_str());
        } else if (remove(s.c_str()) == 0) {
            printf("[ORPHAN-CLEAN]   removed sibling: %s\n", s.c_str());
        }
    }
}
```

---

## 七、决定不做的：秒传时写数据库

"秒传"= 你传一个文件，服务端发现"这文件内容之前有人传过"（md5 一样），就直接告诉你"传完了"，不用再传一遍。

本来我想在秒传时往数据库插一条"这是你的视频"的记录。你说"不用了，多个用户传同一个视频不现实"。所以**没做**。
代价（你已接受）：你自己重传一个以前传过的文件时，"我的视频"列表里不会出现新条目。换来的是逻辑简单、不会有"两个人共享同一个文件"的复杂情况。

---

## 八、部署到 Linux 踩的 5 个坑（大白话）

工程是用 `qmake` 构建的（Qt 那套）。

1. **patch 打错目录**：patch 里的路径对应的是内层 `MediaServer/` 目录，不是外层。打错层就报"找不到文件"。要在内层目录运行 `patch -p1`。
2. **busy 修复已打过**：再打同一个 patch 会提示"已经应用过"。选跳过，别选"应用"（否则会把好改动反向还原）。
3. **qmake 找不到**：系统里 `qmake` 是个空壳，指向没装的版本。用 `find / -name qmake` 找到真正路径 `/home/colin/Qt5.9.9/5.9.9/gcc_64/bin/qmake`，用全路径调用。
4. **端口被占**：启动报"地址已占用"，因为旧的 MediaServer 还在跑。`pkill -9 -f MediaServer` 杀掉再启动。
5. **命令粘一起**：把两条命令粘成一个词（如 `./MediaServerls`），系统找不到文件。一条命令一行，别带提示符粘贴。

---

## 九、最后验证在干啥（造假文件，看它会不会乱删）

我们故意造了几个假文件，模拟"各种状态的半截视频"混在一起：

| 造的文件 | 模拟的现实 | 该不该删 |
|----------|-----------|----------|
| `zz_stale1.part`（195天前） | 半年前传一半、再也没续传 | ✅ 孤儿，该删 |
| `zz_stale1.meta`（195天前） | 它的进度文件 | ✅ 跟着删 |
| `zz_stale2.meta`（195天前） | 另一个被放弃的半成品 | ✅ 孤儿，该删 |
| `zz_fresh1.meta`（刚建） | 正在传 / 刚传一半还没放弃 | ❌ 留着 |
| `zz_normal.flv`（成品） | 早就传完的正式视频 | ❌ 死也不能动 |

**为什么"造出来又删掉"就说明对了？**
这是做个实验：我们埋了"该删的"和"绝不能删的"混在一起。跑完一看，只有那 3 个过期的被删了，`.flv` 和新鲜的 `.meta` 原封不动 —— 说明清理功能**精准只收拾被放弃的半成品，没碰真视频**。要是它把 `.flv` 也删了，那就出大事了，所以"没动 .flv"才是验证的核心。

**为什么分两步（先演习后真删）？**
- 第一步开"演习模式"：只看日志报告"我会删这 3 个"，磁盘上一个不动，零风险，先用人眼确认判断对不对。
- 确认判断没问题，第二步才关掉演习、真删，再看磁盘结果跟报告一致。
- 简单说：**第一步证明"它知道该删谁"，第二步证明"它真的只删了它说的那些"。**

---

## 十、核心函数速查（带注释）

### 上传握手：决定"全新传 / 接着传 / 秒传 / 忙"
```cpp
void CLogic::UploadV2Rq(sock_fd clientfd, char *szbuf, int nlen)
{
    STRU_UPLOAD_V2_RQ *rq = (STRU_UPLOAD_V2_RQ*)szbuf;
    STRU_UPLOAD_V2_RS rs;
    rs.m_nType = _DEF_PACK_UPLOAD_V2_RS;
    rs.m_nResult = upload_v2_fail;     // 先默认"失败"
    rs.m_nTaskId = 0;
    rs.m_nResumeFrom = 0;

    // 0. 基本校验：用户id、文件大小、md5长度都得合法
    if (rq->m_UserId <= 0 || rq->m_nFileSize <= 0 || strlen(rq->m_szMd5) != 32) {
        m_tcp->SendData(clientfd, (char*)&rs, sizeof(rs));
        return;
    }

    // 1. 秒传判断：这个 md5 之前传过吗？（会话内索引里有就直接秒传）
    {
        std::lock_guard<std::mutex> lk(m_v2Mutex);
        auto it = m_mapMd5ToFinalPath.find(rq->m_szMd5);
        if (it != m_mapMd5ToFinalPath.end() && access(it->second.c_str(), F_OK) == 0) {
            rs.m_nResult = upload_v2_instant;      // 秒传！不用再传
            rs.m_nResumeFrom = rq->m_nFileSize;
            m_tcp->SendData(clientfd, (char*)&rs, sizeof(rs));
            return;
        }
    }

    // 2. 占用判断：同一个用户+同一个文件正在传吗？
    char busyKey[160];
    snprintf(busyKey, sizeof(busyKey), "%d_%s", rq->m_UserId, rq->m_szMd5);
    {
        std::lock_guard<std::mutex> lk(m_v2Mutex);
        auto bit = m_mapBusy.find(busyKey);
        if (bit != m_mapBusy.end()) {
            if (time(NULL) - bit->second < 1800) {  // 30 分钟内还占用 → 真有人在传
                rs.m_nResult = upload_v2_busy;      // 报忙，拒绝第二个
                m_tcp->SendData(clientfd, (char*)&rs, sizeof(rs));
                return;
            }
            m_mapBusy.erase(bit);   // 超过 30 分钟 → 视为超时，先解锁再允许
        }
    }

    // ... 中间查用户名、拼路径、建 FileInfo（省略）...

    int64_t resumeFrom = 0;
    rs.m_nResult = upload_v2_new;

    // 3. 续传 or 新建：看有没有 .meta
    if (access(info->m_szMetaPath, F_OK) == 0) {
        UploadMeta meta;
        if (ReadMeta(info->m_szMetaPath, &meta)            // 读出进度
            && strcmp(meta.m_szMd5, info->m_szMd5) == 0     // md5 对得上
            && meta.m_nReceived <= info->m_nFileSize) {
            info->pFile = fopen(info->m_szPartPath, "rb+"); // 打开已有的 .part
            if (info->pFile) {
                fseek(info->pFile, 0, SEEK_END);
                int64_t sz = ftell(info->pFile);
                info->m_nReceived = (sz < meta.m_nReceived) ? sz : meta.m_nReceived;
                resumeFrom = info->m_nReceived;    // 告诉客户端"从第几字节接着发"
                rs.m_nResult = upload_v2_resume;   // 续传！
            } else {
                remove(info->m_szMetaPath);        // .part 丢了，退回新建
            }
        } else {
            remove(info->m_szMetaPath);
            remove(info->m_szPartPath);
        }
    }
    if (rs.m_nResult != upload_v2_resume) {
        info->pFile = fopen(info->m_szPartPath, "wb");  // 没有 → 新建 .part
        info->m_nReceived = 0;
        resumeFrom = 0;
        rs.m_nResult = upload_v2_new;
    }
    if (!info->pFile) {
        delete info;
        m_tcp->SendData(clientfd, (char*)&rs, sizeof(rs));
        return;
    }

    // 4. 分配任务号、登记、加占用锁
    {
        std::lock_guard<std::mutex> lk(m_v2Mutex);
        info->m_nTaskId = m_nNextTaskId++;
        m_mapTaskIdToFileInfo[info->m_nTaskId] = info;
        m_mapBusy[busyKey] = time(NULL);   // 占用锁计时开始
    }
    rs.m_nTaskId = info->m_nTaskId;
    rs.m_nResumeFrom = resumeFrom;
    m_tcp->SendData(clientfd, (char*)&rs, sizeof(rs));  // 把结果回给客户端
}
```

### 收一块数据：定位写 + 记进度
```cpp
void CLogic::UploadBlockRqV2(sock_fd clientfd, char *szbuf, int nlen)
{
    STRU_UPLOAD_BLOCK *rq = (STRU_UPLOAD_BLOCK*)szbuf;
    if (rq->m_nLen <= 0 || rq->m_nLen > _DEF_CONTENT_SIZE) return;  // 块长度不合法
    if (nlen < (int)(sizeof(STRU_UPLOAD_BLOCK) + rq->m_nLen)) return;

    FileInfo* info = NULL;
    {
        std::lock_guard<std::mutex> lk(m_v2Mutex);
        auto it = m_mapTaskIdToFileInfo.find(rq->m_nTaskId);  // 按任务号找到文件
        if (it == m_mapTaskIdToFileInfo.end()) return;
        info = it->second;
    }

    char* payload = (char*)szbuf + sizeof(STRU_UPLOAD_BLOCK);  // 跳过头部，拿到真实数据
    // 只拒"跳块"（offset 超过已收的）；offset 小于等于已收的允许覆盖（重传不翻倍）
    if (rq->m_nOffset > info->m_nReceived) return;

    fseek(info->pFile, rq->m_nOffset, SEEK_SET);              // 定位到该写的位置
    size_t w = fwrite(payload, 1, rq->m_nLen, info->pFile);   // 写盘
    if (w != (size_t)rq->m_nLen) return;                      // 写失败就放弃这块
    if (rq->m_nOffset + rq->m_nLen > info->m_nReceived)
        info->m_nReceived = rq->m_nOffset + rq->m_nLen;        // 推进"已收"指针
    fflush(info->pFile);
    WriteMeta(info);                                          // 把进度写进 .meta（供续传）
    info->m_nLastActive = time(NULL);
    {
        std::lock_guard<std::mutex> lk(m_v2Mutex);
        char busyKey[160];
        snprintf(busyKey, sizeof(busyKey), "%d_%s", info->m_nUserId, info->m_szMd5);
        m_mapBusy[busyKey] = time(NULL);   // 刷新占用锁时间（证明还活着）
    }
    // 收完没在这儿处理，等客户端发"结束"包再终验
}
```

### 传完终验：校验 MD5，通过才落盘
```cpp
void CLogic::FinalizeUpload(sock_fd clientfd, FileInfo* info, int taskId)
{
    STRU_UPLOAD_END_RS rs;
    rs.m_nType = _DEF_PACK_UPLOAD_END_RS;
    rs.m_nResult = 0;

    char calc[33];
    Md5OfFile(info->m_szPartPath, calc);             // 重算 .part 的 MD5
    if (strcmp(calc, info->m_szMd5) != 0) {
        // 校验失败（丢包/损坏）：删掉 .part/.meta，释放，让客户端重传
        m_tcp->SendData(clientfd, (char*)&rs, sizeof(rs));
        remove(info->m_szPartPath);
        remove(info->m_szMetaPath);
        CleanupTask(taskId);
        return;
    }

    // 校验通过：.part 改名为正式视频文件
    if (rename(info->m_szPartPath, info->m_szFilePath) != 0) {
        if (errno == EXDEV) {     // 跨磁盘 rename 不行，就拷贝过去
            if (!CopyFileRaw(info->m_szPartPath, info->m_szFilePath)) {
                m_tcp->SendData(clientfd, (char*)&rs, sizeof(rs));
                remove(info->m_szPartPath); remove(info->m_szMetaPath); CleanupTask(taskId); return;
            }
            remove(info->m_szPartPath);
        } else {
            m_tcp->SendData(clientfd, (char*)&rs, sizeof(rs));
            remove(info->m_szPartPath); remove(info->m_szMetaPath); CleanupTask(taskId); return;
        }
    }
    remove(info->m_szMetaPath);   // .part 已变正式文件，.meta 没用了，删

    // 写数据库（非 gif 类型）
    if (strcmp(info->m_szFileType, "gif") != 0) {
        char sqlBuf[_DEF_SQLIEN] = "";
        sprintf(sqlBuf,
            "INSERT INTO t_VideoInfo (userId, videoName, picName, videoPath, picPath, rtmp, food, funny, ennegy, dance, music, video, outside, edu, hotdegree) values (%d, '%s', '%s', '%s', '%s', '%s', %d, %d, %d, %d, %d, %d, %d, %d, %d);",
            info->m_nUserId, info->m_szFileName, info->m_szGifName, info->m_szFilePath, info->m_szGifPath, info->m_szRtmp,
            info->m_Hobby[0], info->m_Hobby[1], info->m_Hobby[2], info->m_Hobby[3], info->m_Hobby[4], info->m_Hobby[5], info->m_Hobby[6], info->m_Hobby[7], 0);
        m_sql->UpdataMysql(sqlBuf);
    }

    // 记录到会话内秒传索引（同会话再传同 md5 直接秒传）
    {
        std::lock_guard<std::mutex> lk(m_v2Mutex);
        m_mapMd5ToFinalPath[info->m_szMd5] = info->m_szFilePath;
    }
    rs.m_nResult = 1;                              // 成功
    m_tcp->SendData(clientfd, (char*)&rs, sizeof(rs));
    CleanupTask(taskId);                           // 收尾
}
```

---

## 十一、当前状态

- 服务端已经在 Linux 上跑起来了。
- **⚠️ 注意两份源码的 `ORPHAN_DRYRUN` 不一致**：Windows 这份（`D:\newproject`）是 `1`（演习模式，只报告不删）；而 Linux 部署机（`~/MediaServer-duandian`）的启动日志没有 `(DRY-RUN)` 字样，说明那份是 `0`（真删生效）。下次从 Windows 同步代码到 Linux 重编时，孤儿清理会**退回演习模式**——如果想保持真删，同步后记得把 `clogic.cpp` 里 `#define ORPHAN_DRYRUN` 改回 `0`。
- 断点续传全功能可用：busy 卡死修好了 + 客户端调试已撤回 + 孤儿清理验证通过。
- 秒传写库按你的决定没做。

## 十二、后续修复补记（2026-07-15 之后的加固，与 v2 相关的部分）

| 修复 | 说明 |
|------|------|
| `m_nNextTaskId` 未初始化 | 构造函数漏了 `m_nNextTaskId = 1;`，taskId 用的是栈垃圾值（可能为负）。已补。 |
| 服务端收包防崩溃 | `recv_task` 长度头循环读满 4 字节 + `nPackSize` 校验 `(0,10MB]` + 半包丢弃。此前恶意长度可 `new char[2G]` 打挂服务端。 |
| 包最小长度表 | `DealData` 分发前按类型查 `GetMinPackLen()`，短包不再进 handler 越界读。v2 的三个包类型都登记在表里。 |
| 下载 64 倍带宽放大 | `_DEF_CONTENT_SIZE` 提到 64KB 后，v1 下载路径（`DownloadRq`/`UploadHistoryRq`）仍 `fread 1024` 却发整个 64KB+ 结构体。已改满块读取 + 变长发送。 |
| 上传历史包类型漏设 | `UploadHistoryRq` 复用 `STRU_DOWNLOAD_RS` 但没改 `m_nType`，一直顶着 DOWNLOAD_RS 类型发——客户端修好"按来源落控件"后历史页彻底空白才暴露。已补 `rs.m_nType = _DEF_PACK_UPLOADHISTORY_RS;`（与 PlayReport 包类型漏设是同一性质的坑，这个项目已经踩了两次，根治方案是构造函数里写死类型）。 |

仍待做（老清单遗留）：取消上传后 busy 锁 30 分钟死锁（需 CANCEL 包或同用户接管）、`.meta` 每块重写（应节流）、`SendData` 无锁、上传暂停按钮 UI。
