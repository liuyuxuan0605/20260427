# MediaServer-live / MediaPlay-live 面试技能梳理

> 基于对 `MediaServer-live`（C++ 信令服务端）和 `MediaPlay-live`（Qt+FFmpeg 客户端）两个项目源码的实际阅读整理。
> 目标：帮你理清项目里"真正用到"的技术点，讲清楚**做了什么、为什么这么做、有什么取舍**，并提前准备好面试官大概率会追问的薄弱环节。

---

## 0. 一句话项目介绍（可直接当自我介绍开场）

> "这是一个 C++/Qt 实现的音视频点播+直播系统，分服务端和客户端两个工程。服务端用原生 epoll+线程池实现了一个支持自定义二进制协议的信令服务器，负责账号、文件上传下载、推荐列表和直播信令；客户端用 Qt+FFmpeg+OpenGL 实现录制、点播播放、屏幕/摄像头采集，并通过 RTMP 把直播画面推到 nginx-rtmp 服务器，播放端复用同一套解码链路拉流播放。控制面（自定义 TCP 协议）和数据面（RTMP 音视频流）是分离的两条通路。"

这句话本身就体现了你理解"控制面/数据面分离"这个流媒体系统的核心架构思想，面试官听到会很舒服。

---

## 1. 需要掌握的技能地图

按面试考察频率从高到低排列，**每一项都建议对照下面对应章节把"实现细节+为什么+弱点"三件套准备好**：

| 优先级 | 技能 | 对应模块 |
|---|---|---|
| ★★★★★ | epoll 网络编程（LT/ET、EPOLLONESHOT、Reactor模型） | MediaServer `block_epoll_net` |
| ★★★★★ | TCP 粘包/拆包处理，自定义应用层协议设计 | MediaServer + MediaPlay 双端 `packdef.h` |
| ★★★★★ | 线程池原理（生产者消费者、条件变量、动态扩缩容） | MediaServer `Thread_pool` |
| ★★★★★ | FFmpeg 解封装/解码/编码全流程 API | MediaPlay `videoplayer.cpp` / `savevideofilethread.cpp` |
| ★★★★☆ | 音视频同步原理（PTS/DTS、音频主钟） | MediaPlay `videoplayer.cpp` |
| ★★★★☆ | RTMP/FLV 推拉流原理 | MediaPlay `livedialog.cpp`、overview.md |
| ★★★★☆ | Qt 多线程模型（QThread、信号槽跨线程、Worker-Object） | MediaPlay 全局 |
| ★★★☆☆ | OpenGL 基础渲染（纹理贴图） | MediaPlay `opengl/myopenglwidget.cpp` |
| ★★★☆☆ | MySQL C API 使用、SQL 设计 | MediaServer `Mysql.cpp` / `clogic.cpp` |
| ★★★☆☆ | 设计模式（单例、中介者、Worker-Object） | 两个项目都有 |
| ★★☆☆☆ | 网络安全基础（SQL注入、弱口令哈希、鉴权缺失） | 面试官常用来考察工程判断力 |

---

## 2. MediaServer-live（服务端）技术详解

### 2.1 整体架构：epoll + 线程池的"半同步半异步"模型

**不是纯 Reactor，是 half-sync/half-reactor**：

```
epoll_wait 主循环（单线程，只做事件探测，不做阻塞IO）
   → EPOLLIN 触发 → 丢进线程池任务队列（Producer_add）
        → worker 线程内【阻塞】recv 数据（先读4字节长度，再循环recv读满包体）
              → 按协议号查函数指针表 → 调用 CLogic 对应处理函数
```

- 监听 socket 是非阻塞 + epoll **LT（水平触发）**模式（代码里 `EPOLLET` 被注释掉，是刻意选择，不是漏配）。
- 客户端 socket **没有**设为非阻塞——`recv()` 是在线程池 worker 线程里阻塞完成的。这是"牺牲纯异步的极致性能，换实现简单"的工程取舍，能讲出这个取舍逻辑比死记 Reactor 定义更有说服力。
- 用 **`EPOLLONESHOT`** 防止同一个 fd 被多个线程池线程并发处理（LT 模式下这是必须的），worker 处理完后要记得重新 `eventadd` 挂回监听树，否则该连接永久失活——这是 epoll 多线程编程的经典考点，代码里做对了。

**并发量级**：线程池 `Pool_create(200, 10, 50000)`（最大200线程/最小10线程/任务队列5万），`listen backlog=128`。要能区分清楚"同时挂在 epoll 上的空闲连接数"（理论可到几万，受 `ulimit -n` 限制）和"同时被处理的活跃请求数"（受线程池 200 上限限制）这两个不同量级概念。

**面试追问预案**：

- Q: "为什么不把客户端 socket 也设成非阻塞？" → A: "非阻塞需要自己维护每个连接的读状态机（部分读了多少字节、下次从哪继续），复杂度显著上升；用线程池承接阻塞 IO，靠线程数量换并发，实现简单很多，是从工程复杂度出发的权衡，不是不会写。"

**ET vs LT 该选哪个（展开版，比"背定义"更有说服力）**：

- **LT（水平触发）**：只要 socket 缓冲区还有没读完的数据，每次 `epoll_wait` 都会持续通知；这次没读完，下一轮还会提醒，容错性好。
- **ET（边缘触发）**：只在数据"从无到有"这个边缘通知一次，必须配合**非阻塞 socket + 循环读到 EAGAIN**，一次性把数据掏干净，否则这次没读的数据会一直卡住直到下次新数据到达。实现更复杂，但能减少 `epoll_wait` 的重复唤醒，吞吐上限更高，适合 Redis/Nginx 那种纯单线程非阻塞事件循环。
- **本项目为什么选 LT**：架构是"epoll 单线程只分发事件，真正的 `recv` 丢给线程池 worker 用阻塞方式完成"（半同步半异步模型）。阻塞 socket 配合 ET 是公认的反模式——ET 依赖"通知后立刻能读干净"，但阻塞读可能因为数据没到齐而一直卡住，跟 ET 的设计前提冲突。而且已经用了 `EPOLLONESHOT` 防止同一个 fd 被多线程并发处理，ET 想解决的"重复通知"问题在这里已经被 ONESHOT 解决了，上 ET 没有额外收益，只有额外复杂度。
- **话术**："ET 性能上限更高，但要求非阻塞 socket+循环读到 EAGAIN，适合纯单线程非阻塞事件循环；我这个项目是 epoll 分发+线程池阻塞处理的半同步半异步架构，阻塞读配合 ET 容易出问题，所以选 LT，配合 EPOLLONESHOT 防止同一连接被多线程并发处理，这是跟线程模型匹配的选择，不是不知道 ET。"

### 2.2 自定义 TCP 协议设计

**包格式**：`[4字节包体长度][4字节协议类型][具体业务结构体]`，即长度前缀 + 类型字段的经典设计。

**粘包/拆包处理**：先 `read` 定长 4 字节拿到包体长度，再循环 `recv` 直到读满，这是解决"TCP 是字节流、没有消息边界"问题的标准做法。

**协议扩展方式**：所有协议号在 `packdef.h` 里用 `_DEF_PACK_BASE(10000)+偏移` 统一定义，每个消息对应一个 POD 结构体；新增协议 = 加宏定义 + 加结构体 + 在 `setNetPackMap()` 里注册一行映射到函数指针表——表驱动的分发方式，O(1) 查找。

**关键协议举例**（能报出具体的协议家族说明你真的读过代码）：
- 账号：`REGISTER_RQ/RS`、`LOGIN_RQ/RS`（含 8 个兴趣标签字段，为推荐算法服务）
- 文件传输 v1：`UPLOAD_RQ/RS` + `FILEBLOCK_RQ`（64KB 分块、变长发送）+ `DOWNLOAD_RQ/RS`
- 文件传输 v2（断点续传）：`UPLOAD_V2_RQ/RS`（握手：MD5 指纹 + 服务端分配 taskId + 续传偏移）+ `UPLOAD_BLOCK`（变长块，带显式 int64 offset）+ `UPLOAD_END_RQ/RS`（整文件 MD5 终验），支持秒传/续传/busy 占用锁
- 直播信令：`LIVE_START_RQ/RS`、`LIVE_STOP_RQ/RS`、`LIVE_LIST_RQ/RS` + `LIVE_LIST_END`（流式分包+结束哨兵包的设计，客户端不用提前知道条数就能读完整个列表）

**曾经的弱点与修复（面试官很可能问到，能讲"发现→修复"过程更加分）**：
1. ~~包头本身的 4 字节长度读取没有做"读不满则循环"的保护~~ → **已修**：`recv_task` 现在循环读满 4 字节长度头，短读不再导致字节流错位。
2. ~~`nPackSize` 未校验直接 `new char[nPackSize]`~~ → **已修**：服务端收包校验 `0 < nPackSize <= 10MB`（与客户端 `_DEF_MAX_PACK_SIZE` 一致），畸形/恶意长度直接断连，消灭 `new char[2G]`/负数崩溃入口；包体没收满（半包）也不再送进业务层。
3. ~~各 handler 直接强转结构体，短包越界读~~ → **已修**：`DealData` 分发前按包类型查"最小长度表"（`GetMinPackLen`），短包/未注册类型直接丢弃。
4. `recv_task` 里"接收"和"业务处理"本设计想通过线程池二次分发解耦（代码里有一行被注释掉的 `Producer_add(Buffer_Deal, ...)`），实际改成了同步直接调用。**后果要讲到位**：大文件下载在 worker 线程里同步 `fread`+`send` 一路发完，线程池最小才 10 线程，10 个人同时下载就占满全部常驻 worker，登录/直播信令全排队——"服务器没崩但所有人都卡住"。被问"接收和处理是否分离"如实说现状，并点破这个饿死风险。
5. **SIGPIPE 会杀进程（原文档漏、面试高频、必现）**：`send()` 没带 `MSG_NOSIGNAL`、进程也没 `signal(SIGPIPE,SIG_IGN)`。客户端下载中途关窗，服务端往断开连接 send 收到 SIGPIPE，默认动作**终止整个进程**。这是"服务端要对客户端任意断开免疫"的反例，修复=IGN SIGPIPE + send 带 MSG_NOSIGNAL。
6. **登录重复发包（已修，适合讲定位过程）**：`LoginRq` 成功分支发一次响应、函数末尾又无条件发一次 → 登录成功客户端弹**两次**"登录成功"（失败只弹一次）。从"为什么只有成功弹两次"这个不对称现象反推，定位到服务端成功分支多发一个包。已删重复发送、统一由末尾发一次。
7. 仍未修的：SQL 拼接注入面（文件名/标题直接 sprintf 进 SQL）、`_DEF_SQLIEN=400` 长文件名拼 SQL 栈溢出面、文件名未过滤 `../` 路径穿越面、v1 老 map 无锁、`OnClientDisconnect` 不清 v1 老路径——知道在哪、怎么修，面试如实说明即可。

### 2.3 线程池实现（手写，pthread）

**任务队列**：定长数组实现的**环形队列**（`queue_front`/`queue_rear`/`queue_cur` 三指针取模维护）。任务是 `{函数指针, void* arg}` 的 C 风格封装（非 `std::function`）。

**同步机制**：一把互斥锁 `lock` 保护队列状态，配合两个条件变量：`not_full`（队列满时生产者等待）、`not_empty`（队列空时消费者等待）——标准的**生产者消费者模型**。

**动态扩缩容**（这是亮点，很多人写的线程池是固定大小的）：专门有个 **Manager 线程**每 10 秒检查一次负载：
- 扩容条件：队列积压任务数超过"存活-忙碌"的线程数，或忙碌线程占比 ≥ 80%，且未达上限 → 批量建线程。
- 缩容条件：忙碌线程数×2 < 空闲线程数，且存活数大于下限 → 唤醒空闲线程让其自行退出。

能完整讲出这套"80%忙碌率扩容、忙闲比缩容"的判断逻辑是很扎实的加分项。

**面试追问预案**：
- Q: "为什么用函数指针而不是 `std::function`？" → A: 更早期/更贴近 C 的实现风格，`std::function` 有额外的类型擦除开销，函数指针+void*参数更轻量，如果重写会考虑用 `std::function+bind` 提升可读性和灵活性（可以传 lambda）。

### 2.4 数据库访问（MySQL C API，无 ORM）

- 直接用 `mysql_init/mysql_real_connect/mysql_query/mysql_store_result` 系列 C API。
- **没有连接池**：全局唯一一条 `MYSQL* conn`，所有线程共享，用一把全局互斥锁串行化所有 SQL 操作——这是**明显的并发瓶颈**，200 线程的线程池在数据库这一层被拉平成单线程执行，值得主动指出并给出改进方案（每线程独立连接/连接池、或换用支持连接池的 Connector）。
- **SQL 拼接方式**：清一色 `sprintf` 字符串拼接，**存在 SQL 注入漏洞**（用户名、直播标题等字段未转义直接拼进 SQL）；且 `_DEF_SQLIEN` 只有 400 字节，长文件名拼 INSERT 有栈溢出风险。
- **密码是加盐 SHA256（不是明文）**：`crypto_util.cpp` 里 `genSalt()`（16 字节随机→hex）+ `sha256Hex(salt+password)`，DB 存 hash+salt，登录时同法算 hash 再 `strcmp` 比对。⚠️**注意纠正旧口径：不是"明文存储/直接 strcmp 密码"**，早期文档那个说法是错的。真正的弱点是 **SHA256 无迭代/无 KDF**（不像 bcrypt/PBKDF2 那样慢），DB 泄露后可被 GPU 暴力/彩虹表攻击——这才是准确的、经得起追问的说法。

> 这三点（无连接池/SQL注入/密码哈希无迭代）是面试官最容易抓住深挖的安全和工程弱点，**建议不要等被问，面试时主动提"我知道这几个问题，如果继续迭代会这样改"**，这比被动挨问更能体现工程判断力。

### 2.5 直播信令实现（clogic.cpp）

- `LiveStartRq`：查用户名 → 生成 `streamKey = "user_"+用户ID`（**固定规则，无随机 token，存在被冒名推流风险**）→ 先删旧记录再插入新的 `t_LiveStream` 行（`status=1` 表示直播中）→ 把 `streamKey` 返回给客户端。
- `LiveStopRq`：一条 `UPDATE t_LiveStream SET status=0` 软删除。
- `LiveListRq`：`JOIN` 查询所有 `status=1` 的直播，**逐条发送**一个包，最后发一个带 `count` 的结束哨兵包——这是一种不需要客户端提前知道数量就能读完整个列表的流式协议设计。
- **重要**：`overview.md` 设计文档提到的内存态 `LiveRoom` 结构体和 `m_mapLiveIdToRoom` 映射，在实际 `clogic.h/cpp` 里**并不存在**——实际实现把直播间状态完全落到了 MySQL 表里，用数据库代替了内存 map。**面试前务必确认这一点并统一口径**，如果被问"LiveRoom 内存结构怎么实现的"，如实回答"后来改成用数据库表持久化，重启不丢状态，但因为前面提到的全局单连接+大锁，会让直播开播/查询成为数据库热点"。
- MediaServer 本身**不接触音视频字节**，RTMP 推拉流完全交给 nginx-rtmp 处理，服务端纯粹是控制面/信令服务器——可以类比 SIP 信令服务器 + RTP 媒体服务器的关系来讲这个架构分层。

---

## 3. MediaPlay-live（客户端）技术详解

### 3.1 整体架构

分层：网络层（`netapi/net` 原生 socket）→ 中介者层（`netapi/mediator`，解耦网络实现与业务）→ 协议层（`packdef.h`）→ 业务/UI 层（各 Dialog）；另有解码播放（`videoplayer`）、采集编码（`picinpic_read`/`audio_read`/`savevideofilethread`）、渲染（`opengl/`）三条独立链路。

**中介者模式（Mediator Pattern）**：`INet`（`TcpClient`/`UdpNet`）收到数据不直接碰 UI，而是调用 `INetMediator::DealData()` 再 `emit SIG_ReadyData(...)`，业务层只依赖 mediator 接口——换底层传输方式（TCP↔UDP）不需要改业务代码。

### 3.2 线程模型（重点，面试必问"你的多线程是怎么设计的"）

| 线程 | 实现方式 | 职责 |
|---|---|---|
| 网络接收线程 | `_beginthreadex`（原生线程，非 QThread） | 死循环阻塞 `recv()`，收到整包后通过信号槽跨线程投递给 UI |
| 视频读包线程 | `QThread`（`VideoPlayer::run`） | `av_read_frame` 读包，按流类型分发进 `videoq`/`audioq` |
| 视频解码线程 | SDL 原生线程（`SDL_CreateThread`） | 从队列取包、解码、`sws_scale` 转 RGB |
| 音频解码线程 | SDL 音频回调线程（`audio_callback`） | 解码+重采样+混音，由 SDL 音频子系统驱动 |
| 采集线程 | `QThread`（`PicInPic_Read`） | 摄像头/桌面画面采集 |
| 编码写文件/推流线程 | `QThread`（`SaveVideoFileThread`） | 消费采集数据，编码+封装+写文件/RTMP推流 |
| 上传线程 | Worker-Object（`moveToThread`） | 文件分块上传，不阻塞 UI |

**跨线程通信的核心机制**：网络线程、解码线程都不直接操作 UI，而是通过 **Qt 信号槽 + `Qt::AutoConnection`**（跨线程自动退化为 `QueuedConnection`）把数据"扔"进主线程事件队列，不需要手动加锁，网络线程可以继续处理下一个包。这是这个项目里最核心、最应该讲清楚的并发范式。

**同线程内部高频结构用锁+条件变量**：`PacketQueue`（SDL_mutex/SDL_cond）、`SaveVideoFileThread` 的双队列（`QMutex` 保护，但**消费端用 `msleep(1)` 轮询而非条件变量通知**——是一个可以被追问的简化实现，诚实回答"确实没用条件变量，用了轮询，会有轻微延迟和空转，如果优化会换成条件变量通知"）。

**内存所有权约定**（细节题，能答出来说明你真的读代码而不是背概念）：网络线程 `new` 出来的 `buf` 转发给多个 Dialog 处理时，只有一处负责 `delete[]`，其余处只读不释放，避免 double free。

### 3.3 FFmpeg 播放全流程

```
avformat_alloc_context → avformat_open_input → avformat_find_stream_info
→ 遍历流找视频/音频 stream_index → avcodec_find_decoder + avcodec_open2
→ 循环 av_read_frame 读包 → 按 stream_index 分流进 PacketQueue
→ 视频线程: packet_queue_get → avcodec_decode_video2 解码 YUV
    → sws_scale(SWS_BICUBIC) 转 RGB32 → QImage → 信号发给 UI
→ 音频回调: avcodec_decode_audio4 解码 FLTP → swr_convert 重采样成 S16SYS
    → SDL_MixAudioFormat 混音输出
```

**注意**：用的是 `avcodec_decode_video2`/`avcodec_decode_audio4` 这类 **FFmpeg 3.x 及更早的旧 API**（4.2.2 里已标记 deprecated），如果被问"为什么不用 `avcodec_send_packet/receive_frame`"，可以答"这是当时教程/参考代码遗留的旧接口，新接口支持一个 packet 输出多帧、错误处理更清晰，如果重构会升级"。有意思的是编码侧（`SaveVideoFileThread`）反而用了新 API，说明新旧代码是不同阶段写的。

**软解码，非硬解**：`avcodec_find_decoder` 没有指定硬件解码器名字（如 `h264_cuvid`/`d3d11va`），走纯 CPU 解码路径。

### 3.4 音视频同步

- **以音频时钟为主钟**（人耳对音频卡顿更敏感，是行业惯例）。
- 视频线程解码前忙等：若 `video_pts > audio_pts` 说明视频超前，`SDL_Delay(5)` 等音频追上；若视频落后则直接渲染（**没有主动丢帧追赶逻辑**，这是同步机制的不完善之处，长时间可能有累积的轻微不同步，面试常被问到，可以诚实说明并提出改进方向：视频落后超过阈值时跳过若干帧不渲染）。
- `synchronize_video()` 用 `repeat_pict` 做重复帧时间补偿，是标准 ffplay 早期实现的同步算法移植。
- **Seek 实现**：`av_seek_frame(AVSEEK_FLAG_BACKWARD)` 跳到目标点之前最近的关键帧 → 投递 `FLUSH` 哨兵包让解码线程 `avcodec_flush_buffers()` 清空内部缓存 → 逐帧比较 pts 丢弃早于目标时间的帧直到追上。**关键帧相关的知识点**（GOP=12，25fps 约 0.48 秒一个关键帧；seek 只能精确到关键帧，中间时间点靠"跳到关键帧+丢帧"来逼近）是本项目一个非常适合展开讲的考点。

### 3.5 渲染：OpenGL 固定管线纹理贴图

- SDL2 只负责音频播放和线程调度，**画面渲染走的是 OpenGL**（`QOpenGLWidget`），不是 SDL renderer。
- **YUV→RGB 转换在 CPU 完成**（`sws_scale`），OpenGL 层拿到的已经是转换好的 RGB32 数据，只做 `glTexImage2D` 整帧上传纹理 + `glBegin(GL_QUADS)` 画四边形贴图，**没有用可编程管线/shader**，也没有做 YUV 纹理+片元着色器转换的 GPU 加速。这是一个**明显的可优化点**，面试常问"为什么不用 shader 做 YUV→RGB"，可以直接承认这是待优化项，讲清楚优化方向（三个 YUV 平面各建一张纹理，fragment shader 里做色彩空间转换，把 CPU 密集的 swscale 开销转移到 GPU，大分辨率/高帧率场景收益明显）。
- 用 `QSize::scaled(Qt::KeepAspectRatio)` 计算等比缩放后换算 NDC 坐标画四边形，实现保持宽高比居中显示。

### 3.6 直播模块：零侵入复用录制代码

这是整个项目里**最值得重点讲**的设计，体现你对 FFmpeg 协议层抽象的理解：

- `LiveDialog` 直接复用 `SaveVideoFileThread`（新建一个独立实例，与录制线程互不干扰），唯一区别是把 `format.fileName` 设成 `rtmp://` 开头的 URL 而不是本地文件路径。
- `avformat_alloc_output_context2(&oc, NULL, "flv", filename)` **显式指定 FLV 封装格式**（RTMP 协议本身只能承载 FLV tag 格式的音视频数据，这是标准写法），随后 `avio_open` 会根据 URL 的 `rtmp://` scheme **自动切换到 FFmpeg 内建的 RTMP protocol handler**，整个推流切换对编码逻辑完全透明，不需要改一行编码代码——这就是 overview.md 里说的"FLV→RTMP 无缝切换"，本质是利用了 FFmpeg AVIOContext 的协议自动分发机制。
- 观众端播放直播流复用了**同一套 `VideoPlayer` 点播播放逻辑**，直播和点播用的是同一个解码器，没有为直播单独写代码。
- 信令复用 `OnlineDialog` 已建立的 TCP 长连接，不新开连接；`LiveDialog` 只读不释放收到的网络 buffer，`OnlineDialog` 统一回收，避免 double free。
- **线程生命周期管理正确**：`stopLivePush()` 严格按"通知线程退出 → `wait()` 阻塞等真正退出（此时 `av_write_trailer`/`avformat_free_context` 已在线程内跑完）→ 才 `delete`"的顺序,避免了线程未退出就释放 FFmpeg 上下文导致野指针的经典 bug，这个细节能讲出来是加分项。
- **已知问题（面试前建议确认或坦白）**：
  1. `overview.md` 提到的 `m_bIsLive` 标志和 `avformat_network_init()` 调用，在当前 `savevideofilethread.h/.cpp` 源码里**实际不存在**——这是文档与代码不一致，需要面试前确认清楚。FFmpeg 使用网络协议前理论上需要 `avformat_network_init()` 初始化（Windows 上主要是 WinSock），如果代码里确实缺失是个隐患。
  2. 直播编码分辨率取的是整个屏幕分辨率，但直播设定是"只推摄像头不录屏"，逻辑上不一致（应该用摄像头采集分辨率），是个可深挖的小 bug。
  3. `streamKey` 由服务端生成为 `"user_"+userId`，无鉴权 token，理论上任何知道对方 userId 的人都能拼出其推流地址冒名推流。

### 3.7 客户端 TCP 协议实现

- 发送端：一次 `send()` 把 4 字节长度+包体拼一起发出，同时禁用 Nagle 算法（`TCP_NODELAY`），降低小信令包延迟。
- 接收端：先阻塞读 4 字节长度，再循环 `recv` 读满包体——**注意：只有包体做了循环读满，4 字节长度头本身是单次 `recv`（`TcpClient.cpp:164`），没循环读满**。所以严格说粘包/半包只处理了一半，长度头跨 TCP 段的极端情况没防（服务端已修这一点、客户端没修，两端不对称）。
- **已知弱点**：内层读包体的循环对 `recv` 返回 <=0 有判断（不会死循环，这点比早期版本好）；但 `nPackSize` 上界校验有、**长度头短读没防**；`SendData` 返回 `send()` 原始值，失败的 -1 被 `bool` 转成 true，调用方 `if(...<0)` 判断失效（断网上传要等超时才发现）；**没有自动重连和心跳保活**，断线检测依赖 `recv` 返回 0，拔线等场景可能长时间无法感知——被问"断线重连怎么做"如实说明现状是被动的（下次发送时才重连），改进思路是心跳 PING/PONG + 主动重连。
- ⚠️**别把"元类型没注册"当严重 bug 讲**：客户端只 `qRegisterMetaType<Hobby>`，跨线程信号确实传了裸 `char*`/`uint8_t*` 没注册。但**实测能正常登录收包**，说明当前 Qt 构建下 `char*` 队列投递能跑通，它只是可移植性隐患，不是"收不到包"。讲成致命 bug 会被"那你怎么还能登录"一句问穿。

---

## 4. 直播推拉流协议选型：RTMP vs HLS

**结论（针对本项目）**：继续用 RTMP 更合适，没必要换成 HLS——不是"不知道 HLS"，是这两个协议解决的问题不一样，选型要看播放端是谁、场景规模多大。

### 核心区别

| | RTMP | HLS |
|---|---|---|
| 传输方式 | 基于 TCP 的长连接，持续推/拉音视频流 | 基于 HTTP，把视频切成一段段 .ts/.m4s 分片 + .m3u8 播放列表 |
| 延迟 | 低，通常 1-3 秒 | 高，传统 HLS 通常 6-30 秒（分片时长+缓冲导致），LL-HLS 能压到 2-5 秒但实现复杂 |
| 是否支持"推流/ingest" | 支持（编码端持续推数据） | **不支持**——HLS 是纯拉流协议，没有"HLS 推流"这个概念，编码端必须先用 RTMP/RTSP/SRT 推给服务器，服务器再切片转成 HLS 给观众拉 |
| 播放端兼容性 | 需要支持 RTMP 的播放器（FFmpeg、专用播放器），现代浏览器原生不支持（Flash 时代的协议） | 浏览器/iOS 原生支持（`<video>`标签、Safari），移动端友好 |
| CDN 缓存友好度 | 差（长连接，不好用 HTTP CDN 缓存） | 好（本质是普通 HTTP 静态文件，CDN 天然支持，适合大规模分发） |

### 为什么本项目适合继续用 RTMP，不适合换 HLS

1. **播放端是自己写的桌面客户端（MediaPlay），不是浏览器**：HLS 相比 RTMP 最大的优势是"浏览器/iOS 原生支持，不用装播放器"——但播放端本来就是拿 FFmpeg 写的播放器，FFmpeg 对 RTMP 和 HLS 的支持一样成熟，这个优势不成立。
2. **推流端必须还是 RTMP，躲不掉**：HLS 没有"推流"能力，就算观众端想拉 HLS，主播端的 `SaveVideoFileThread` 依然得先用 RTMP 把流推到服务器，服务器再转切片——协议链路里 RTMP 一步都省不掉，只是多加一层"转 HLS"的复杂度。
3. **延迟是直播最核心的体验指标**：HLS 传统方案 6-30 秒延迟，对"观众看主播直播"这种需要实时感的场景是明显劣化；现在端到端走 RTMP，观众端延迟能做到 1-3 秒，这个优势不该主动放弃。
4. **HLS 真正的优势场景是"大规模、多平台分发"**：比如要同时支持网页观看、iOS App 观看、CDN 扛几十万并发观众，这时候 HLS（甚至加 LL-HLS）的 CDN 缓存友好和浏览器兼容性才有意义。本项目场景是"自己的客户端、观众规模不大"，这个优势用不上。

### 面试话术

> "我推拉流两端都用 RTMP，没有换 HLS，是因为 HLS 本质是拉流协议，推流环节还是得用 RTMP，等于多一层转切片的复杂度换不来收益；而且 HLS 典型延迟在 6-30 秒，对直播的实时性是明显劣化。HLS 真正的优势是浏览器原生支持和 CDN 缓存友好，适合大规模、多平台分发的场景，但我这里播放端是自己写的桌面客户端，用 FFmpeg 直接拉 RTMP 完全够用，延迟还更低，所以 RTMP 对我这个场景是更合适的选择。如果以后要支持网页观看或者扩大到 CDN 级别的并发，会考虑加一路 HLS 输出（nginx-rtmp 模块本身就支持推流的同时切片输出 HLS），但不会替换掉 RTMP。"

---

## 5. 通用面试问题预案（结合本项目作答要点）

### Q: 讲一下你的项目
按"0. 一句话项目介绍"展开，然后按"服务端：epoll+线程池+MySQL" → "客户端：Qt多线程+FFmpeg+OpenGL" → "直播模块怎么把两边打通的" 三段式展开，2-3分钟版本。

"这是一个 C++/Qt 实现的音视频点播+直播系统，分服务端和客户端两个工程。服务端用原生 epoll+线程池实现了一个支持自定义二进制协议的信令服务器，负责账号、文件上传下载、推荐列表和直播信令；客户端用 Qt+FFmpeg+OpenGL 实现录制、点播播放、屏幕/摄像头采集，并通过 RTMP 把直播画面推到 nginx-rtmp 服务器，播放端复用同一套解码链路拉流播放。控制面（自定义 TCP 协议）和数据面（RTMP 音视频流）是分离的两条通路。"

### Q: 为什么用 TCP 不用 HTTP？
自定义二进制协议，包体更紧凑（不用像 HTTP 那样带一堆文本头），长连接下减少握手开销，更适合这种"客户端服务端都是自己写、协议私有"的场景。

### Q: 项目最大的技术难点是什么？
建议答"直播模块的音视频链路打通"或"epoll+线程池的并发模型设计"，具体展开时要能讲出真实的取舍过程（比如为什么客户端 socket 选择阻塞模式、为什么直播能做到零侵入复用录制代码）。

### Q: 项目有什么不足，如果重构你会怎么改？
**主动抛出**（不要等被问），按"会炸的 → 安全 → 性能 → 体验"排序更显工程判断力：
1. 服务端会炸的：**SIGPIPE 没屏蔽（客户端关窗一次 send 就杀进程）**、大文件同步发送占满线程池饿死信令、v1 老 map 无锁并发、断连不清 v1 老路径。
2. 服务端安全：SQL 注入、`_DEF_SQLIEN=400` 栈溢出、密码是加盐 SHA256 但无迭代（可暴力）、无鉴权 token 可冒充、路径穿越、数据库无连接池。
3. 客户端会炸的：`VideoPlayer::run()` 异常路径不复位标志导致停止时 GUI 死循环、无音频流走定时器路径卡死 + UAF、`SendData` 返回值语义错发送失败当成功。
4. 客户端体验/性能：音画同步无追帧逻辑、YUV→RGB 没上 GPU shader、断线无自动重连/心跳、废弃 FFmpeg 解码 API、每帧重建 SwrContext。
这份坦诚清单本身就是本文档的价值——"我知道问题在哪、按严重度排过序、也知道怎么改"，比一味夸项目更让面试官信服。**其中登录重复发包、分包长度头这类已修的，讲"发现→定位→修复"过程；未修的讲"知道怎么修"。**

### Q: 并发是怎么保证的？
服务端：epoll 单线程分发事件 + 线程池多线程处理业务（避免慢连接拖死事件循环）；数据库层因为全局单连接+锁，是真实的并发瓶颈，要如实说明。
客户端：多线程分工明确（网络/解码/渲染/编码各自独立线程），线程间用信号槽（跨线程场景）或锁+条件变量（同线程高频结构）同步。

---

## 6. 面试前的 Checklist

- [ ] 能不看代码，口头画出服务端 epoll+线程池+MySQL 的完整请求处理链路图
- [ ] 能不看代码，口头画出客户端播放一个视频涉及的所有线程和它们之间的数据流向
- [ ] 能解释清楚 TCP 粘包问题本质，以及项目里用了什么方案解决
- [ ] 能解释清楚 PTS/DTS、关键帧、GOP 的关系，以及 seek 为什么需要"跳关键帧+丢帧"
- [ ] 能讲清楚直播模块"零侵入复用录制代码"的原理（FLV muxer + AVIOContext 协议自动分发）
- [ ] 提前想好上面列出的所有"技术债/弱点"该怎么回答，不要被问懵
- [ ] 确认 `overview.md` 中 `LiveRoom`/`m_mapLiveIdToRoom`、`m_bIsLive`/`avformat_network_init()` 两处与当前代码不一致的地方，统一口径（是否后续改了实现、还是文档超前于代码）
- [ ] 过一遍项目自带的 `MediaPlay-live/MediaPlay/面试问题答案.md`、`docs/av_sync.md`、`docs/decode_pipeline.md`、`docs/live_stream_feature.md`，与本文档交叉核对

---

## 附：项目内已有相关文档索引

- `overview.md`（根目录）：直播功能架构设计文档
- `MediaPlay-live/MediaPlay/面试问题答案.md`：项目作者自己整理的 10 道面试真题问答
- `MediaPlay-live/MediaPlay/docs/av_sync.md`：音视频同步专题
- `MediaPlay-live/MediaPlay/docs/decode_pipeline.md`：解码流程专题
- `MediaPlay-live/MediaPlay/docs/live_stream_feature.md`：直播功能专题（1088行，最详细）
- `MediaPlay-live/MediaPlay/docs/音视频基础知识合集.md` / `音视频编码流程讲解.md`：基础知识
- `MediaPlay-live/MediaPlay/docs/项目开发逐字稿.md`：开发过程记录
