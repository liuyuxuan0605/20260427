# MediaServer-live / MediaPlay-live 项目简介

> 一个基于 C/S 架构的 Linux 高并发音视频媒体服务平台，包含流媒体服务端（C++ / epoll）与桌面客户端（Qt / FFmpeg / SDL）。覆盖注册登录、音视频**断点续传**上传下载、直播推拉流、基于标签与热度的内容推荐的完整业务链路。（曾做过点赞/收藏功能，已整体回退，简历勿写。）

---

## 一、项目定位

- **服务端 `MediaServer-live`**：运行在 Linux 上的 C++ 高并发媒体服务器，采用 `epoll`（LT）+ `EPOLLONESHOT` + POSIX 线程池模型处理海量长连接，负责注册/登录、音视频断点续传上传下载、直播信令中转、基于标签匹配的推荐计算与播放行为驱动的热度体系。
- **客户端 `MediaPlay-live`**：基于 Qt5 的跨平台桌面客户端，使用 FFmpeg 完成解封装/解码、SDL2 完成音视频渲染，通过自研 TCP 二进制协议与服务端通信，提供推荐浏览、播放、上传（可断点续传/秒传）、直播观看等交互。

> 一句话概述（面试/简历可用）：**"一个 Linux 上的 C++ 高并发媒体服务器（epoll + 线程池处理上万连接，负责注册登录、音视频断点续传上传下载、直播信令与标签+热度推荐），配套 Qt + FFmpeg + SDL 实现的桌面播放器。"**

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|---|---|---|
| 服务端语言 | C++11 | 多线程、RAII、裸指针管理为主 |
| 服务端网络 | epoll（LT + EPOLLONESHOT） | 高并发 I/O 多路复用（代码中 EPOLLET 注释未启用） |
| 服务端并发 | POSIX 线程池（pthread） | Manager + Worker 动态扩缩容 |
| 服务端存储 | MySQL C API | 单连接 + 全局锁封装 |
| 流媒体 | RTMP | 直播推流 / 点播 URL 拼接 |
| 文件传输 | 自研断点续传协议 | MD5 指纹 + taskId + 64KB 变长块 + offset 定位写 + .part/.meta + 终验 |
| 客户端 UI | Qt5 | 信号槽、QToolButton、QStackedWidget |
| 客户端解码 | FFmpeg | 解封装 / 音视频解码（H.264 + AAC） |
| 客户端渲染 | SDL2 | 音频回调 + 视频帧上屏 |
| 通信协议 | 自定义二进制 TCP | 4 字节长度前缀 + 包类型 + 结构体 |

---

## 三、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      MediaPlay-live (Qt Client)              │
│  ┌──────────┐   ┌──────────────┐   ┌──────────────────────┐  │
│  │ UI 层    │──▶│ TcpClient    │   │ videoplayer          │  │
│  │(online-  │   │(分包/校验)   │   │(FFmpeg+SDL 播放)     │  │
│  │ dialog)  │   └──────┬───────┘   └──────────────────────┘  │
└─────────────┼──────────┼────────────────────────────────────┘
              │  TCP 自定义二进制协议（长度前缀 + 包类型）
              ▼
┌─────────────────────────────────────────────────────────────┐
│                    MediaServer-live (Linux)                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ TCPKernel / block_epoll_net                          │   │
│  │  epoll(LT+ONESHOT) → 线程池派发 → recv_task         │   │
│  └───────────────────────┬──────────────────────────────┘   │
│                          ▼                                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ CLogic（业务 Handler 分发）                          │   │
│  │  Register / Login / UploadV2(断点续传) / Download / │   │
│  │  Live / Recommend / PlayReport ...                  │   │
│  └──────┬───────────────────────────┬──────────────────┘   │
│         ▼                       ▼                            │
│  ┌────────────┐          ┌──────────────┐   ┌──────────┐   │
│  │ CMysql     │          │ 文件系统/视频 │   │ RTMP 服务│   │
│  │(MySQL C API)          │ 存储          │   │          │   │
│  └────────────┘          └──────────────┘   └──────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**数据流要点**：客户端请求 → `TCPKernel` 经 epoll 唤醒 → 投线程池 `recv_task` 读满整包 → `CLogic::DealData` 按包类型分发到对应 Handler → Handler 调 `CMysql` 落库 / 读写文件 / 生成 RTMP 地址 → `SendData` 回包。

---

## 四、服务端核心模块

| 模块 | 文件 | 职责 |
|---|---|---|
| 网络内核 | `TCPKernel` / `block_epoll_net.cpp` | epoll 事件循环、accept、读事件派发、发送；收包零信任校验（长度头循环读满、`(0,10MB]` 上界、半包丢弃、按类型最小长度表） |
| 线程池 | `Thread_pool.cpp` | Worker 处理任务队列，Manager 动态扩缩容 |
| 业务逻辑 | `clogic.cpp` | 注册/登录/断点续传上传/下载/直播/推荐/播放上报等 Handler |
| 数据访问 | `Mysql.cpp` | MySQL C API 封装（查询/更新/结果集释放） |
| 协议定义 | `packdef.h` | 包类型枚举、请求/响应结构体（如 `STRU_UPLOAD_V2_RQ`） |

### 服务端技术亮点
- **高并发 I/O 模型**：`epoll`（LT）+ `EPOLLONESHOT`，保证同一 fd 同一时刻只被一个线程处理，避免多线程并发读同一连接。
- **业务与 I/O 分离雏形**：读事件由 epoll 线程分发至线程池，业务逻辑（DB、文件）在 worker 中执行，epoll 线程不被阻塞。
- **自研二进制协议 + 零信任收包**：长度前缀分包，包类型驱动 Handler 路由；恶意/畸形长度直接断连，短包按类型最小长度表拦截。
- **断点续传 v2**：整文件 MD5 指纹识别续传身份，64KB 变长块 + 显式 offset 定位写（重发幂等），`.part`/`.meta` 双文件 + 原子 rename 落盘，整文件 MD5 终验防静默损坏；支持秒传、busy 占用锁、断连锁回收、孤儿临时文件 7 天 TTL 启动清理（dry-run 演习模式防误删）。
- **推荐与热度**：标签匹配(7坑位)+纯热度(3坑位)防信息茧房；"看过才排除"+ LRU 补位 + 换一批轮转；热度由播放上报单源驱动（近30天完播率中枢惩罚式公式）。

---

## 五、客户端核心模块

| 模块 | 文件 | 职责 |
|---|---|---|
| 网络层 | `TcpClient` | TCP 连接、分包（含长度上界校验、半包循环读）、收包回调 |
| 播放器 | `videoplayer.cpp` | FFmpeg 解封装/解码、SDL 音视频渲染、音视频同步 |
| 交互 UI | `onlinedialog.cpp` | 推荐/上传历史双页卡片展示、刷新换一批、上传（worker 线程 + 握手等待）、直播观看入口 |

### 客户端技术亮点
- **音视频同步（音频主时钟）**：以音频时钟为基准，视频帧 `video_pts <= audio_pts` 才渲染，追帧保证口型/画面同步，是业界标准做法。
- **Seek 防花屏**：`seek` 时下发 `FLUSH_DATA` 清空解码器与队列缓存，避免残帧导致花屏。
- **跨线程安全传图**：视频帧经 `QImage` 信号传到 Qt 主线程上控件（隐式共享 + copy 后安全），不直接走 SDL 窗口。
- **网络层比服务端起步更稳**：`RecvData` 一开始就有长度上界校验与半包循环读；服务端后来对称补齐了同等校验（可讲"客户端做了、服务端漏了→统一加固"的过程）。

---

## 六、核心功能一览

| 功能 | 服务端 | 客户端 |
|---|---|---|
| 注册 / 登录 | `RegisterRq` / `LoginRq`（SHA256+salt 存储） | 表单提交、会话维护 |
| 音视频上传（断点续传） | `UploadV2Rq`（MD5握手/秒传/busy）+ `UploadBlockRqV2`（offset定位写）+ `UploadEndRqV2`（MD5终验） | 算整文件MD5、worker线程分块发送、断点续传、可取消 |
| 点播下载 | `DownloadRq` / `GetFileList`（64KB满块变长发送） | 列表浏览、播放 |
| 上传历史 | `UploadHistoryRq`（按热度排序） | 历史页卡片展示 |
| 直播 | `LiveStartRq` / `LiveStopRq` + RTMP 地址 | 观看 RTMP 流 |
| 推荐 | `GetFileList` 四级填坑（标签+热度+LRU补位+换一批） | 刷新按钮、卡片点击播放 |
| 行为上报 | `VideoClickRq`（写"看过"表）/ `PlayReportRq`（重算热度） | 点击/播放结束自动上报 |

---

## 七、典型业务流程：断点续传上传

1. 客户端选文件 → worker 线程算整文件 MD5 → 发 `UPLOAD_V2_RQ` 握手。
2. 服务端 `UploadV2Rq`：查秒传索引（命中直接返回"已完成"）→ 查 busy 锁（同用户同文件在传则拒绝）→ 读 `.meta` 决定续传偏移 → 分配 taskId 回复。
3. 客户端 `seek(resumeFrom)` 循环发 64KB 变长块（带 taskId + offset）；服务端定位写 `.part`、每块更新 `.meta`。
4. 客户端发 `UPLOAD_END_RQ` → 服务端对 `.part` 重算 MD5 与指纹比对 → 通过则原子 rename 为正式文件、写 `t_VideoInfo`、回成功。
5. 中途断网/取消：不发 END，服务端断连回调只清内存锁、保留 `.part`/`.meta`，下次同文件从断点续传；7 天不回来则被启动清理扫掉。

> 曾踩坑：异常断连时 busy 锁不释放导致 30 分钟内无法续传（网络层与业务层解耦、断线无回调），通过给网络层挂 `OnClientDisconnect` 回调修复。

---

## 八、工程演进方向（已知改进点）

为展示工程素养，以下为基于代码审查梳理的待优化项（非缺陷清单，而是改进路线）：

1. ~~零信任分包~~ **已完成**：服务端长度前缀循环读满 + `(0,10MB]` 上界校验 + 半包丢弃 + 按包类型最小长度表；仅剩 `ntohl` 字节序归一待补。
2. **并发安全**：v1 共享 `std::map`（文件/会话状态）加锁或收敛到 per-connection 对象（v2 断点续传新状态表已全部 `mutex` 保护）；`SendData` 按 fd 加发送锁。
3. **鉴权与会话**：登录下发 token（带超时），业务包校验 token→userId，杜绝无鉴权冒充。
4. **安全加固**：SQL 参数化 / `mysql_real_escape_string`；文件名白名单 + 服务端重命名（UUID）；路径 `realpath` 归一化防穿越。
5. **数据库**：引入连接池 + 超时/重连，缓解单连接串行化瓶颈。
6. **断点续传收尾**：上传取消/暂停按钮 UI、`.meta` 写盘节流（当前每块都写）、秒传索引持久化（当前重启失效）。
7. **客户端升级**：FFmpeg 解码 API 升级到 `send_packet`/`receive_frame`；复用 `SwrContext`；统一错误路径释放资源。

---

## 九、适用场景

- 校内/局域网音视频点播与直播平台原型
- C++ 高并发网络编程、Qt 多媒体客户端、FFmpeg/SDL 播放器学习的完整示例
- 面试中讲解「epoll 并发模型」「音视频同步」「自定义协议设计」的实战项目载体
