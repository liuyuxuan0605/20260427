# 密码加密方案（SHA-256 + 随机盐）

> 记录本次给 `MediaServer-live` 账号系统加密码哈希的完整思路，供简历话术、面试展开使用。

---

## 一、简历话术

**精简版（项目经历列表里用，一行）**：

> 通过代码审计发现服务端账号系统密码明文存储漏洞，设计并实现基于 SHA-256 + 随机盐的哈希存储方案，替换原有明文比对逻辑，杜绝拖库场景下用户密码泄露风险。

**展开版（项目描述/项目亮点段落里用）**：

> 在自定义 TCP 协议服务端的账号模块中，通过审计代码发现密码采用明文存储、登录时直接 `strcmp` 明文比对，存在数据库泄露即密码全量泄露的安全隐患。调研对比 MD5、SHA-256、bcrypt 等主流哈希方案的安全性与实现成本后，选定 **SHA-256 + 每账号独立随机盐** 的方案：基于 OpenSSL 封装了盐生成与哈希计算工具模块，改造注册/登录逻辑使密码"仅以不可逆哈希值+盐"形式落库，登录时重新计算哈希比对而非比对明文，同步完成数据库表结构调整与构建配置改造。

**一句话面试开场（如果被问"你做过哪些安全相关的改进"）**：

> "我审计出项目账号系统密码明文存储的问题，主动做了 SHA-256 加盐哈希改造，能讲清楚为什么选哈希不选加密、为什么要加盐、以及跟 bcrypt 这类专用密码哈希算法的取舍。"

---

## 二、知识点梳理

### 1. 哈希（Hash） vs 加密（Encryption）—— 概念上不能说反

这次做的严格来说是**哈希**，不是"加密"（虽然口语里都叫"密码加密"）：
- **加密**是可逆的：有对应的解密算法/密钥能把密文还原成明文（比如 AES）。
- **哈希**是单向不可逆的：只能从明文算出哈希值，理论上无法从哈希值反推出明文（除非撞库/暴力枚举）。

密码存储场景**必须用哈希，不能用加密**——如果用可逆加密，服务端一定持有密钥，一旦服务端被攻破，所有密码可以被解密还原，等于白做；哈希则算法本身不可逆，攻破服务端也拿不到明文密码。

### 2. 为什么一定要加盐（Salt）

如果只是 `hash(password)` 不加盐，会被两种方式攻破：
- **彩虹表攻击（Rainbow Table）**：攻击者提前算好"常见密码 → 哈希值"的巨大对照表，拿到哈希值直接查表还原，不用现算。
- **相同密码 → 相同哈希**：两个用户用了同一个密码，哈希值也会完全一样，一旦有一个被破，另一个也暴露；还能被攻击者用来批量识别"哪些用户用了弱密码"。

**盐（salt）** 是每个用户注册时随机生成的一小段数据，跟密码拼在一起再哈希：`hash(salt + password)`，盐和哈希值一起存库。作用：
- 相同密码因为盐不同，哈希结果也不同，彻底破坏"一张表打天下"的彩虹表攻击。
- 攻击者想破解，必须针对**每个用户单独**重新暴力枚举（用他专属的盐），攻击成本从"破一次等于破全部"变成"破一个只等于破一个"。

（本项目实现：`crypto_util.cpp` 的 `genSalt()` 用 OpenSSL `RAND_bytes()` 生成16字节密码学安全随机数，转32位十六进制字符串。）

### 3. 为什么用随机数生成盐，不能用简单的 `rand()`

C 标准库的 `rand()` 是伪随机数生成器，种子和算法固定的话理论上**可预测**，不适合用在安全场景。本项目用的是 OpenSSL 的 `RAND_bytes()`，属于**密码学安全的随机数生成器（CSPRNG）**，专门为安全用途设计，不可预测性有保证。

### 4. 哈希算法的"快"是把双刃剑

SHA-256 被设计成**运算速度很快**（本来是为了文件校验、数字签名等场景服务的，追求高吞吐），但这个"快"用在密码哈希上反而是缺点：攻击者可以用 GPU/专用硬件对哈希值做**海量暴力枚举**（每秒能试几十亿次），如果用户密码本身比较弱（比如"123456"），加了盐也照样能被针对性暴力破解出来，只是没法用彩虹表批量破解了。这是 SHA-256 方案相比专用密码哈希算法（bcrypt/scrypt/Argon2）的核心弱点，下面对比表会展开。

### 5. `strcmp` 比较哈希值是否安全

登录校验时用 `strcmp(storedHash, calcHash)` 比较两个哈希字符串，这里有一个更深的知识点：`strcmp` 是**逐字节比较，遇到不相等就提前返回**，属于非"恒定时间比较"（non-constant-time），理论上存在**时序攻击（Timing Attack）**风险——攻击者通过精确测量比较耗时的微小差异，可以推断出密码哈希前几位是否猜对，逐位爆破。生产级系统会用专门的恒定时间比较函数（如 OpenSSL 的 `CRYPTO_memcmp`）来避免。**这是我们这次方案里还没解决的已知细节，属于更进阶的安全知识点**，面试中如果对方追问到这个深度，说明这是可以主动指出的"进一步加固方向"。

---

## 三、为什么选 SHA-256 + 盐，而不是别的方案

### 核心原因

1. **对这个项目定位来说"够用"**：这是一个 C++ 教学/练手项目，核心目的是证明"我知道密码不能明文存、知道要加盐防彩虹表"，SHA-256+盐已经能完整体现这个安全意识，不需要上生产级最优解。
2. **零额外依赖**：SHA-256 和随机数生成都在 OpenSSL 的 `libcrypto` 里，Linux 服务器基本都自带或者 `apt install libssl-dev` 一条命令的事；bcrypt/Argon2 在 C++ 里没有标准库支持，需要额外引入第三方库（`libbcrypt`、`libsodium` 等），对一个本来就很轻量的手写服务端项目而言，多引入一个依赖的工程代价不成比例。
3. **实现和讲解成本低**：一个哈希函数 + 一个随机数函数就能讲清楚整个方案，面试展开也不会陷入"这个库怎么用"的细节，聚焦在"为什么这么设计"这个更有价值的讨论上。

### 权衡（诚实的短板，面试可以主动提）

SHA-256 本质上还是一个"通用哈希算法"，不是"专用密码哈希算法"，最大短板是**运算太快、不抗暴力破解**（见上面知识点4）。如果是生产级系统，更推荐 bcrypt/Argon2 这类**故意设计得很慢、可调计算成本**的算法。

---

## 四、主流方案对比

| 算法 | 类型 | 输出长度 | 计算速度 | 是否为密码设计 | 抗暴力破解能力 | 是否需要额外依赖(C++) | 综合评价 |
|---|---|---|---|---|---|---|---|
| **MD5** | 通用哈希 | 128位 | 极快 | 否 | 弱（已有实用碰撞攻击，且太快） | 内置/OpenSSL自带 | 已不推荐用于任何安全场景，仅兼容老系统用 |
| **SHA-1** | 通用哈希 | 160位 | 快 | 否 | 弱（已有实用碰撞攻击，2017年SHAttered证明） | 内置/OpenSSL自带 | 同样不推荐，已被主流机构淘汰 |
| **SHA-256（不加盐）** | 通用哈希 | 256位 | 快 | 否 | 中等偏弱（无碰撞攻击，但太快，抗暴力破解弱，且无盐怕彩虹表） | 无（OpenSSL自带） | 单独用不够，必须配合盐 |
| **SHA-256 + 随机盐（本项目采用）** | 通用哈希+盐 | 256位 | 快 | 否 | 中等（防住了彩虹表，但暴力枚举单个账号仍然较快） | 无（OpenSSL自带） | 教学/中小项目够用，实现简单，安全意识达标 |
| **bcrypt** | 专用密码哈希 | 184位(通常以固定格式字符串存储) | 慢（可调 cost 因子） | 是 | 强（专门设计成慢，GPU加速收益有限） | 需要第三方库（如 `libbcrypt`） | 行业主流推荐，工程成熟，缺点是C++生态里不算标准库 |
| **scrypt** | 专用密码哈希 | 可配置 | 慢（同时消耗内存，抗ASIC/GPU更强） | 是 | 更强（内存密集型，专用硬件破解成本更高） | 需要第三方库（`libsodium`等） | 安全性比bcrypt更进一步，实现和调参复杂度也更高 |
| **Argon2** | 专用密码哈希 | 可配置 | 慢（可调时间/内存/并行度三个成本因子） | 是（2015年密码哈希竞赛冠军） | 最强（当前公认的密码哈希最佳实践） | 需要第三方库（`libargon2`/`libsodium`） | 新项目的首选，但生态对老旧C++项目不算友好 |

### 一句话总结每个的定位

- **MD5/SHA-1**：过时，只用于非安全场景（比如文件校验、生成缓存key），绝不能用于密码。
- **SHA-256（裸用）**：安全性介于"能用"和"不推荐"之间，必须加盐才有基本安全性。
- **SHA-256+盐（本项目）**：教学/练手项目的合理选择，讲清楚了核心安全意识（不可逆存储+防彩虹表），零依赖、易实现。
- **bcrypt/scrypt/Argon2**：生产级系统的正确答案，专门为"故意算得慢"而设计，能有效拖慢暴力破解，缺点是 C++ 里都要引入第三方库，实现和调参成本比 SHA-256 高一截。

### 如果面试官问"为什么不直接用 bcrypt"

> "如果是生产系统我会优先选 bcrypt 或 Argon2，因为它们是专门为密码存储设计的慢哈希算法，能有效对抗 GPU 暴力破解；这次选 SHA-256+盐主要是工程权衡——项目是 C++ 手写的轻量服务端，bcrypt 在 C++ 里没有标准库实现，需要额外引入第三方依赖，而 SHA-256 通过 OpenSSL 的 `libcrypto` 就能直接用，零额外依赖成本。如果要往生产级演进，下一步就是换成 bcrypt。"

---

## 五、涉及代码位置（方便回忆细节）

- `MediaServer-live/MediaServer/include/crypto_util.h` / `src/crypto_util.cpp`：`genSalt()`（生成盐）、`sha256Hex()`（计算哈希）
- `MediaServer-live/MediaServer/src/clogic.cpp`：`RegisterRq`（注册时生成盐+哈希后落库）、`LoginRq`（登录时取出盐重新计算哈希后比较）
- `MediaServer-live/MediaServer/src/makefile` / `MediaServer.pro`：链接 `-lcrypto`
- 数据库：`t_UserData` 表新增 `salt` 字段，`password` 字段存的是64位十六进制哈希值（不是明文）
