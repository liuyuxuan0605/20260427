# MediaServer 项目 — 面试介绍版（候选人视角）

> 用途：面试开口讲项目用。分「30 秒开场」「2 分钟展开」「追问预案」「自我反思」四层。
> 核心策略：**先一句话定位 → 主动挑 1~2 个最有把握的亮点展开 → 把"踩过的坑/待改进"作为反思主动抛出来**，让面试官看到"自己知道哪里有问题"的成熟度。

---

## 一、30 秒电梯演讲（开口必说，背熟）

> "我做过一个运行在 Linux 上的 C++ 高并发音视频媒体平台。服务端用 **epoll + EPOLLONESHOT + 线程池** 处理高并发连接，负责注册登录、音视频**断点续传**上传下载、直播信令和基于标签的推荐；客户端是一个 **Qt + FFmpeg + SDL** 实现的桌面播放器，支持音视频同步播放和直播观看。我在里面主要负责（网络并发模型 / 播放器解码同步 / 业务 Handler）这块。"

（注意：不要说"点赞收藏"——该功能已整体回退，简历/口述里别留这个词，被追问演示就尴尬了。断点续传是现在最有讲头的新亮点，见亮点 C。）

说完停顿，等面试官选方向。如果对方没打断，自然进入下面 2 分钟版。

---

## 二、2 分钟深度展开（挑 1~2 个亮点，别全讲）

### 可选亮点 A：高并发网络模型（推荐，含金量最高）

"服务端的并发模型是我觉得最有意思的部分。

- 我用 **epoll 水平触发（LT）+ EPOLLONESHOT**（注意：代码里 `EPOLLET` 是被注释掉的，实际跑的是 LT——别说成边缘触发，面试官翻代码或追问 ET 的 while-read-drain 写法就露馅）：EPOLLONESHOT 保证同一个 fd 同一时刻只被一个 worker 线程处理，处理完再 re-arm，避免同一连接被多线程并发读；LT 模式配合 ONESHOT 也不怕漏事件。
- 读取到的连接事件**投到 POSIX 线程池**处理，epoll 线程只负责 accept 和事件分发，不被业务阻塞。
- 业务 Handler（登录、上传、下载、直播）在线程池 worker 里跑，通过 `CLogic::DealData` 按包类型路由。
- **收包做了零信任加固**（后来补的，可作为"发现→修复"案例）：4 字节长度头循环读满、`nPackSize` 校验 `(0,10MB]`、半包丢弃、按包类型查最小长度表拦短包。

这里有个**设计权衡**我后来复盘过：EPOLLONESHOT 只保证『同一个 fd 不被并发处理』，但不同 fd 的业务会并发改共享的 `std::map`（文件信息表、用户 fd 表），所以多线程下 v1 的裸 map 其实有数据竞争风险——v2 断点续传的新状态表我都加了 `std::mutex`，v1 老表还没收敛，这是我在代码审查里给自己挑出来的点。"

> 面试官听到"你自己知道裸 map 有 race、新代码已经改对了"会加分，比他挑出来你才慌强得多。

### 可选亮点 C：断点续传 v2（新，工程完整度高）

"上传我从'随机 fileId + 1KB 定长分块'重构成了断点续传：

- **文件身份**：上传前客户端算整文件 MD5 做指纹，服务端靠它认出'这是上次传一半的那个文件'（随机 id 认不出续传，这是旧版的根本缺陷）；
- **握手协议**：`UPLOAD_V2_RQ/RS` 服务端分配 taskId + 返回续传偏移，客户端 seek 后从断点接着发；块改成 64KB 变长（定长头 + 实际 payload，末块不再浪费带宽），每块带显式 int64 offset，服务端定位写、幂等覆盖（重传不翻倍）；
- **落盘安全**：数据写 `.part`，进度写 `.meta`（先写 tmp 再 rename 原子替换），全部收完对 `.part` 重算 MD5 终验，通过才 rename 成正式文件——中途断电/丢包都不会产生静默损坏的成品；
- **并发与清理**：同用户同文件用 busy 锁防并发冲突（断连回调释放锁，修过一个'异常断连锁不释放导致 30 分钟 busy 死锁'的真 bug）；启动时扫描清理 7 天无活跃的孤儿 `.part/.meta`（白名单后缀 + 配对删 + dry-run 演习模式三道防误删保险）。"

### 可选亮点 B：音视频同步（客户端，稳拿分）

"客户端的播放器是我做的另一块。同步策略是**以音频时钟为主时钟**：

- 音频用 SDL 音频回调持续输出，天然形成时间基准；
- 视频帧渲染前比较 `video_pts` 和 `audio_pts`，只有 `video_pts <= audio_pts` 才上屏，否则追帧/丢帧跟上音频。
- `seek` 时下发 `FLUSH_DATA` 清空解码器和队列缓存，避免残帧花屏。

这是音视频播放器的标准做法，但里面**追帧阈值、音频时钟漂移补偿**这些细节是真正容易出 bug 的地方，我专门处理过。"

### 必带一个"我解决的真问题"（证明能排错，可从下面挑一个）

**案例 1：点赞计数不增加（MySQL C API 铁律）**——注意：点赞功能后来已整体回退，讲这个案例时说"当时做过一版点赞功能"，别让面试官以为现在还有。

"这个项目里我独立定位并修过一个挺隐蔽的 bug——**点赞计数不增加**。

- 现象：客户端点爱心，请求明明发出去了（`SendData ok=true`），服务端计数却不变。
- 排查：不是协议字段错位（我核对过两端结构体一致），也不是 DB 表缺列。最后发现是 `Mysql.cpp` 的查询封装**漏调了 `mysql_free_result`**。
- 根因：MySQL C API 规定 `mysql_store_result` 拿到结果集后必须 `mysql_free_result` 释放，否则下一次 `mysql_query` 会报 `CR_COMMANDS_OUT_OF_SYNC` 直接失败。点赞恰好是『SELECT→INSERT/UPDATE→SELECT』连续多语句，前面没释放就把整条链打挂了；而登录/列表是单次查询或纯写入，所以不触发。
- 修复：在结果集遍历后补 `mysql_free_result`，顺手把 NULL 列做空字符串兜底。这个修复至今还在（`Mysql.cpp` 里那行注释就是当时写的），后来点赞功能虽然回退了，但这个底层修复让后面所有多语句业务（推荐、播放上报）都受益。"

**案例 2：包类型漏设导致链路"静默失联"（更推荐，踩了两次的同型坑）**

"播放上报链路一直'发了等于没发'：客户端确认发包成功、服务端却连日志都不打。排查过登录状态、触发时机、编译部署，全不是。最后定位：请求结构体构造函数只 `memset` 清零、不设 `m_nType`，客户端发送前忘了手动赋值，包类型恒为 0；服务端分发入口 `type >= _DEF_PACK_BASE(10000)` 校验直接静默丢弃——业务层根本没收到。补一行 `rq.m_nType = ...` 链路就通了。更有意思的是**同一性质的坑后来又踩了一次**：上传历史复用下载回复结构体、服务端忘了改类型字段，客户端把历史头当下载头处理。两次之后我的结论是：**这类'调用方手动赋值'的约定迟早会漏，应该在构造函数里把类型写死**，从设计上消除整类问题——这也是我对'约定 vs 强制'的一个实际体会。"

> 这两个案例的含金量在于：**从"现象+日志"反推到根因**（MySQL API 铁律 / 协议分发的静默丢弃路径），而不是瞎改代码。

---

## 三、追问预案（面试官常问 + 我的回答要点）

| 面试官可能的追问 | 我的回答要点（不要背，理解后用自己话讲） |
|---|---|
| 服务端为什么裸 `std::map` 没崩？ | 当前测试并发度低 + 运气；本质是 data race，**v2 断点续传的新表已全部 `mutex` 保护，v1 老表待收敛** |
| 服务端收包怎么防恶意包？ | **已修**：长度头循环读满 + `(0,10MB]` 上界校验 + 半包丢弃 + 按类型最小长度表；还差 `ntohl` 字节序归一 |
| 为什么只有一个 MySQL 连接？ | 单连接 + 全局锁能保证线程安全，但全服务 SQL 串行化是瓶颈；**应上连接池 + 超时重连** |
| 音视频同步万一音频卡顿怎么办？ | 主时钟可降级到系统时钟或视频时钟；音频回调加缓冲水位管理 |
| 你们协议怎么防粘包/拆包？ | 4 字节长度前缀 + 包类型；半包循环读满（两端都已做到，服务端是后补的） |
| 断点续传怎么防"续传重发导致文件损坏"？ | 每块带显式 offset 服务端定位写（重发幂等覆盖）+ 整文件 MD5 终验 + `.part`→rename 原子落盘，三层保证 |
| 秒传怎么做的？ | md5→路径索引命中即返回"已完成"；当前索引只在内存（重启失效），持久化到 DB 是待做项 |
| 直播是怎么实现的？ | 服务端生成 RTMP 推/拉流地址（`rtmp://IP/vod/用户/文件`），客户端用 FFmpeg 拉 RTMP 流解码 |
| 注册密码怎么存的？ | `sha256(salt+password)`，但**无迭代、salt 弱**，理想用 bcrypt/argon2；这属于待改进 |

---

## 四、自我反思话术（主动抛，展示工程素养）

"如果让我重做一遍，这几块我会改：

1. **安全**：业务包目前只带 client 给的 userId，无鉴权/会话，能冒充任意用户；应登录下发 token 校验。SQL 用 `sprintf` 拼接有注入风险，要参数化或 `mysql_real_escape_string`；文件名没过滤 `../` 有路径穿越面。
2. **健壮性**：`SendData` 的 `send()` 不循环、不处理 `EAGAIN`、注释说加锁实际没加，大包会部分写丢；应按 fd 加发送锁或单发送线程。
3. **并发**：v1 的共享 map 无锁（v2 新代码已加），应统一收敛。
4. **客户端**：FFmpeg 用的还是 `avcodec_decode_video2` 等已废弃 API，应升级到 `send_packet`/`receive_frame`；`SwrContext` 每帧重建应复用。

我列这些不是项目烂，而是**这个项目是从零搭的完整链路，每个坑我都亲手踩过、也知道标准解法是啥**——而且有几个（收包零信任校验、断连锁泄漏、包类型漏设）我已经完整走过'发现→定位→修复→验证'一轮，这正是它对我价值最大的地方。"

---

## 五、临场心法

- 面试官问"讲讲项目"→ 用 30 秒版，然后**主动**抛亮点 A 或 B，不要等。
- 被问到不会的 → 先说"这块我当时的做法是 X"，再补"标准做法应该是 Y，我还没在生产环境验证过"，**比硬编强**。
- 雷点（裸 map、无鉴权、注入）**自己先说**，就变成加分项；等他问出来就是扣分项。
- 全程用"我"：我设计、我排查、我修、我复盘。
